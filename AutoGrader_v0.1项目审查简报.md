以下是基于您提供的审查报告梳理的 **AutoGrader 项目审查简报**。当前日期为 2026 年 9 月 20 日，距离初赛截止（9 月 26 日 23:59）仅剩 **6 天**，本简报聚焦于快速决策与行动。

---

# AutoGrader 项目审查简报
**审查日期**：2026 年 9 月 20 日  
**项目定位**：计算机专业实验报告自动解析、评分点逐项核查与成绩评语智能生成的智能评阅教学平台（赛事方向一）

## 一、 项目摘要
当前项目处于 **“代码质量极高，但交付状态濒临失败”** 的矛盾状态。  
- **核心优势**：前端工程化水平出众（Next.js 15 + React 19，严格类型检查，0 TODO，0 死链）。采用创新的 "Compile-time AI" 范式，数据高度自洽（Rubric 权重精确、12/12 总分与 144/144 单项分严格复算相等、13/13 结果指纹全通，MAE 3.05 真实成立）。选题与官方示例完全命中，具备极强先手优势。  
- **致命短板**：交付物严重缺失。主链接 404，备用链接为假占位页，核心源码未推送到 Git，且缺失 Demo 视频、PPT 及真实的 AI 工具使用证明。若不及时修正，工程完整性与 AI 工具使用维度将面临断崖式扣分。

---

## 二、 亟待解决的问题（P0/P1 优先级，必须在本周内解决）

### 🔴 P0：交付与部署危机（今日必须解决）
1. **源码未提交，他人无法构建**：`frontend/`、`docs/` 未推送到仓库；且构建强依赖同级的 `demo/` 目录，该目录目前未纳入 Git，导致任何人 clone 后均会因 `ENOENT` 构建失败。
2. **主链接 404，备用链接欺诈**：GitHub Pages 未启用（`has_pages: false`）；当前唯一返回 200 的备用链接是手写占位页，并非真实产物，极易被评委判定为作弊或敷衍。
3. **危险缓存未排除**：`frontend/node_modules/.cache/ag-salvage` 存在自指 junction，若被推送到仓库会导致递归遍历卡死。

### 🟠 P1：赛事合规与评分项缺失（3天内解决）
1. **核心提交物空缺**：缺失 3 分钟 Demo 视频；缺失作品介绍 PPT（**注**：无 PPT 则无法提交团队成员信息，将直接丢失“跨专业组队 +2”加分）。
2. **AI 工具使用证明无效**：LearnBuddy 对话记录（`memory/`）位于外部仓库；当前 12 份结果中的 `sourceConversationId` 为合成串，且完全缺失 `sourceTurnId`，评委无法验证 AI 的实际参与度。
3. **数据契约违规**：`sample-11/R9` 和 `sample-12/R9` 证据数组为空，但 `needsReview` 却为 `false`，违反 `docs/contract.md §3` 规则 1。（**警告**：修复此问题后，必须使用已验证算法重新计算所有 fingerprint，否则 13/13 校验将全红）。

---

## 三、 未完成的功能与待完善项

### 1. 功能局限性（受限于纯静态架构）
- **无交互能力**：当前为 `output: 'export'` 纯静态导出，仅支持成果浏览，**不支持用户上传新报告或重新触发评分**。
- **无浏览器端校验**：缺乏前端一键复算指纹的功能，评委无法在浏览器端直观验证数据的防篡改性。
- **核查器调用遗漏**：`verifyItemScore` 函数已定义但从未被调用。

### 2. 工程清理与文档修正（P2 优先级）
- **README 启动命令存在平台陷阱**：文档提供的 `NEXT_PUBLIC_BASE_PATH= npm run dev` 为 Bash 语法，在 Windows PowerShell 下会导致变量被删除并回退到错误的 `/AutoGrader` 路径，需补充 Windows 兼容命令。
- **过时断言未更新**：README 中仍声称“inspectors 未接线”、“结果运行时拉取”，与实测（已接线、零 fetch 纯构建期读取）严重不符。
- **代码冗余**：存在约 11 个未调用的导出符号、未使用的 UI 组件（如 `separator.tsx`）、45 个符号的死 barrel（`lib/inspectors/index.ts`），以及过期的“尚无结果”注释。
- **无用目录**：`backend/` 为空目录，`build.log` 包含失败记录，需清理。

---

## 四、 倒计时 6 天行动清单 (Action Items)

| 优先级 | 行动项 | 负责人/状态 |
| :--- | :--- | :--- |
| **P0** | 执行 `git add frontend/ demo/ docs/`，配置 `.github/workflows/deploy.yml` 并推送，启用 GitHub Pages。 | ⬜ 待办 |
| **P0** | 将真实的 `out/` 静态产物部署到备用链接，替换当前的占位页。 | ⬜ 待办 |
| **P0** | 检查并确保 `.gitignore` 已排除 `node_modules` 及 `ag-salvage` 缓存。 | ⬜ 待办 |
| **P1** | 录制 3 分钟 Demo 视频，制作包含团队成员信息的 PPT。 | ⬜ 待办 |
| **P1** | 将 LearnBuddy 真实对话记录纳入仓库，替换合成 ID 为真实 `sourceConversationId` 和 `sourceTurnId`，并**重算所有 fingerprint**。 | ⬜ 待办 |
| **P1** | 修复 `sample-11/R9` 和 `12/R9` 的 `needsReview` 逻辑，并**重算 fingerprint**。 | ⬜ 待办 |
| **P2** | 修正 README 中的 Windows 启动命令、过时断言，清理 dead code 与空目录。 | ⬜ 待办 |

> **💡 审查员建议**：项目最大的资产是 `docs/testing.md` 中高质量的量化自证（如主动暴露差档 MAE 8.00）。建议在 PPT 和 Demo 视频中**重点展示这一量化评估过程**，将“静态站”的劣势转化为“工程严谨、结果可复现”的降维打击优势。