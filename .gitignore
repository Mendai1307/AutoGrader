# ============================================================================
# AutoGrader · 仓库根 .gitignore
# ----------------------------------------------------------------------------
# 仓库根 = 整个工作区。交付物三部分（专家智能体包 backend/、Web Demo web/、
# 评审材料 materials/）都在同一个仓库里。
#
# 排除原则：**只排除产物与过程，不排除证据**。
#   · 产物：依赖目录、构建输出、缓存 —— 可由源码重建，不该进库；
#   · 过程：.work/（本轮开发的中间脚本与探针产物）、.learnbuddy/（项目记忆）
#     —— 属开发侧内部资料，不作为评审材料；
#   · 证据（**必须进库**）：documents/ 立项与总纲、contract/ 契约真源、
#     web/public/ 下的数据资产与字体子集 —— 它们是"可复现、可审计"的凭据。
# ============================================================================

# ---- 依赖与构建产物 ----
node_modules/
.next/
out/
out-stage/
*.tsbuildinfo
tsconfig.tsbuildinfo
build.log

# ---- 过程目录（开发侧内部资料，不作评审材料）----
.work/
.learnbuddy/

# ---- 临时与残留 ----
# 同步类脚本的 staging 目录：正常情况不应存在，出现即为上一次失败的残留
*.staging/
*.tmp
*.bak
*~

# ---- 编辑器与系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ---- Office 临时锁文件 ----
# Word 打开 docx 时会生成 ~$xxx.docx；documents/ 下的立项材料必须保持整洁
~$*

# ---- 日志 ----
*.log
npm-debug.log*
