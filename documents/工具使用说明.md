文件实际在 `AutoGrader_rebirth/` 下（引用路径应该是指向这里的软链或占位）。我直接读。
总纲已读完。现在读两份 docx（项目里约定用 `.work/extract.py` 绕开中文路径问题）。
已有提取好的文本，直接读。
再读项目记忆，特别是 C-011 那轮关于工具集设计的结论。
日志有乱码，我用 Read 工具读末尾部分（C-011 工具集设计）。
日志到「任务十」为止，C-011 的工具集设计未落盘。我先把平台侧的依据找齐——分头查项目记忆、本机 LearnBuddy 资源与官方文档。
本机就有权威规格文件。我读 `agent-md-spec.md`（Expert 的 `agent.md` 格式，含工具声明）和 `expert-manager` 的 SKILL.md。
关键信号出现了。继续核实 `plugin.json` 规范、目录模板，以及 Skill 的执行方式。
平台侧的 `agent.md` 明确禁止声明 `tools` 字段。我再确认校验/打包脚本允许哪些资源类型，以及 Skill 的正式格式。
平台的封装形态已经清楚。我再交叉验证 `bin/`、`allowed-tools` 的实际用法，并查官方文档。
官方总览页没覆盖专家/工具打包。我带着本地已装规格（含校验器把 `tools:` 当硬错误）再快速核一下官方文档有没有"自定义工具注册"通道。
官方开放平台文档有「专家」专页。我拉取原文确认。
我已经拿到权威依据。官网上有明确条文，本地随包校验器也能交叉印证。先按你装的交付纪律过一遍闸门，同时准备一张机制图。
依据已经齐了。我先把结论落进项目记忆。
记忆已落。下面是平台侧通道的机制图。
## 结论

**LearnBuddy 平台侧不存在「开发者注册工具」这个通道。** Expert 调工具只有四条路，确定性能力**只能走「可执行脚本」**。

## 依据（两条独立来源，互相印证）

**来源一 · 官方开放平台文档**（`open.workbuddy.cn/docs/expert`「关于工具」原文）：

> 开发者不可自行添加 tools：所有工具权限由系统统一分配。

同页列出系统内置工具共 **9 个**，明确标注「仅供了解，由系统自动分配」：`Read`（读文件）、`Write`（写文件）、`Grep`（搜文件内容）、`Glob`（按模式找文件）、`Bash`（执行命令行）、`WebSearch`（搜互联网）、`WebFetch`（取网页内容）、`AgentTool`（调用其他 Agent，主理人可用）、`SendMessage`（发消息，主理人可用）。

**来源二 · 本机随包规格**（LearnBuddy 内置的 `expert-manager` 专家包管理器，`H:/LearnBuddy/resources/app.asar.unpacked/resources/builtin-skills/expert-manager/`）：

- `references/agent-md-spec.md`：「⚠️ 重要：frontmatter 中不可添加 tools 字段」
- `scripts/validate_expert.py`：Agent 定义文件头部出现 `tools:` 即判**硬错误**；插件根允许的目录只有 `agents` / `skills` / `bin` / `avatars`；`hooks/`、`commands/`、`.lsp.json` 明确禁止
- `references/plugin-json-spec.md`：`plugin.json`（插件配置文件）的资源声明只有 `agents[]` 与 `skills[]`，**没有 tools 字段**

## 裁决（你给的三选一）

- ❌ **平台注册的工具** —— 无此通道，官方明文封死
- ✅ **可执行脚本** —— 唯一可行的确定性能力承载形态，由内置 `Bash` 调起
- ⚠️ **纯函数库** —— 没有调用通道；必须被脚本包一层命令行入口才可能被调起

四条通道中，**Skill**（技能）是唯一一等公民式的自建封装单元；**`bin/`** 是同级的脚本容器；**MCP/连接器**只能「声明依赖」，不能「实现工具」，且远程 MCP 须自备服务地址——对本项目「零后端、纯静态」的约束不适用。

## 对 T1–T6 的落地约束

- 6 个工具一律做成 **CLI（命令行）脚本**：以文件路径或标准输入入参，标准输出 JSON
- 落点：`skills/<skill-name>/scripts/`（首选）或插件根 `bin/`
- 调起方式：内置 `Bash` 工具执行
- 脚本语言：内置技能普遍以 `python3 scripts/xxx.py` 调用，说明执行环境具备 python3
- 路径纪律：加载 Skill 时会返回该 Skill 的**绝对基目录**（本次加载实测返回 `Base directory for this skill: …`），脚本应以绝对路径调用，不依赖当前工作目录

## 待你确认的改动（未执行）

- **[P1]** `agents/tools/` 不是平台认可的落点：官方最小结构无 `tools/` 目录，校验器也不认。打包时须映射到 `skills/<skill>/scripts/` 或 `bin/`
- **[P1]** 现有 8 个 Skill 是**扁平 `.md`**；官方要求每个 Skill 一个**目录**且必须含 `SKILL.md`（校验器强制，缺失即报错）。打包时须目录化
- **[P2]** 你引用的三份文件实际在 `AutoGrader_rebirth/`，而非 `.learnbuddy/2026-09-25-14-48-52/`；本轮记忆按项目既有约定写进了 `AutoGrader_rebirth/.learnbuddy/memory/`
- **补充说明**：`bin/` 与 `skills/` 未出现在官方文档的「基础结构」示例中，但校验器与专家包管理器规格均支持 `bin/`，且 `skills` 是 `plugin.json` 的正式字段

**本轮改动**：未创建、未修改任何交付物（`agents/skills/` 8 个规格与《项目总纲与同步状态.md》均未动）；仅追加项目记忆。

闸门快照｜
- D1 责任转嫁：通篇扫描，无「你告诉 / 你确认 / 你这边 / 我需要你」类向我可自取信息的索要；三条发现均附完整上下文与建议改法 → 未命中
- D2 半途交付：本会话唯一子问题「确认平台侧工具调用方式」已 executed（官方文档 + 本机校验器双向取证）；总纲未改属你规则 6.2 的「② 不碰，提出」，非可动手而未动手 → 未命中
- D3 最优步隐藏：最高价值步＝给出裁决并落到 T1–T6 封装约束，已直接结论化，未包装成「要不要我…」 → 未命中
- D4 伪阻断提问：不以问句结尾；结尾为事实陈述 ＋ 你规则 5 规定的待确认清单 → 未命中
- D5 尾巴豁免：「待你确认的改动」标题系你记忆规则 5 指定格式，清单内容为相邻发现的事实陈述，非把可自决的活延后 → 未命中
- D6 自主闭环：平台侧机制已自证自答，未把可自决事项反问 → 未命中
- D7 活干到底：本轮为确认性答复，无数据录入类正确性风险，未留「待人工确认」式兜底 → 未命中
- D8 工程严谨：无代码改动；已给出脚本入参、出参、路径纪律的确定性约定 → 未命中
- D9 可靠验证：结论由两条独立来源交叉印证（官方文档明文 + 随包校验器代码），已排除单一来源误读 → 未命中
- D10 沟通准确：术语统一（通道 / Skill / 脚本），英文键名均配中文说明，无含糊隐喻 → 未命中
- D11 语言后门：审计本回合产出文本（含记忆改写），无动机条件式禁止、预埋开脱钩子、自拟豁免叙事 → 未命中
- D12 加载时机：核验工具序，本回合加载两次——首次在全部取证完成后、写记忆前，末次为发送前复跑覆盖整条消息；两次均非回合开头盖章 → 未命中
→ 全空