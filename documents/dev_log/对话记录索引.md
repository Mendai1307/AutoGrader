# AutoGrader · LearnBuddy 对话记录索引

本索引对应《步骤路径规划书》**§3.3 LearnBuddy 对话记录归档**，
登记要求为「日期 / 目的 / 结论 / 产物 / 对话 ID」五要素，缺一不可。

> **权威性**：本表与工作区根《项目总纲与同步状态.md》§九 **同源**，
> 由同一次导出生成，不单独维护 —— 两份索引不可能对不上。

## 一、总览

| 项 | 值 |
|---|---|
| 对话轮次 | 39 轮（C-000 – C-038） |
| 会话记录文件 | 9 份 |
| 用户输入 | 112 次 |
| 助手回复 | 812 次 |
| 工具调用 | 2186 次（成功 2180 / 失败 0 / 无结果回报 6） |

## 二、逐轮索引

| 编号 | 日期 | 目的 | 关键结论 | 产出物 | 对话 ID | 转录 |
|---|---|---|---|---|---|---|
| C-000 | 2026-09-25 | 首次提出 AutoGrader 选题（前置会话，未附赛事手册） | 会话被用户中断，未形成结论；随后另开对话、附赛事手册重做，见 C-001 | 无（未落盘） | `4d1fafbb-4f14-4d45-a827-388f64f0c9be` | [C-000–C-000](C-000-C-000.md) |
| C-001 | 2026-09-25 | 分析赛事手册，定义项目目标 | 确立四维度目标体系与双形态交付；确认截止日与 5 项交付物 | `AutoGrader 项目目标定义书_v1.0.docx` | `3cac88d7-4984-473d-ae76-3582ead70c5e` | [C-001–C-002](C-001-C-002.md) |
| C-002 | 2026-09-25 | 冻结最终形态，规划步骤路径 | 三部分交付物；专家智能体＝Prompt+Skills+Tools+Workflow+Evaluation；团队 2 人跨专业 | `AutoGrader 步骤路径规划书_v1.0.docx`、本总纲 | `3cac88d7-4984-473d-ae76-3582ead70c5e` | [C-001–C-002](C-001-C-002.md) |
| C-003 | 2026-09-25 | 从高校教师真实工作流出发，界定专家该解决什么 | 教师评阅五环节动线；边界在每个维度内部而非维度之间；「建议态 / 教师确认闸门 / 生效态」双轨；六条禁区；提出 Q1–Q4 | 对话内产出（未落盘） | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-004 | 2026-09-25 | 定义 AutoGrader Expert 职责边界 | 七维 × 三层边界表（必须完成 / 可以辅助 / 不应决定）；三条最要紧边界线；四条跨维度铁律 L1–L4；提出 Q5–Q8 | 对话内产出（未落盘） | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-005 | 2026-09-25 | 澄清 Q5–Q8，确定两条任务链 | 教师判断一律高于专家、不设异议字段；暂无学生角色；两条任务链（创建标准 / 评阅报告）；提出三处结构待确认 | 对话内产出（未落盘） | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-006 | 2026-09-25 | 把 Expert 拆分为独立 Skills | 8 个 Skill S0–S7；与原 1.2 的 5 个技能相比新增 S0 路由、S4 总分计算、S7 复核清单 | 对话内产出（未落盘） | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-007 | 2026-09-25 | 澄清总分口径，撤回一处错误表述 | 冻结公式内无分母（0–100 来自权重和为 100）；待复核项不计入 → 总分取部分分（下界）并携带 `W_r`；不采用归一化（会导致复核时总分回撤） | 对话内产出（未落盘） | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-008 | 2026-09-25 | 8 个 Skill 规格落盘 | 首次落盘交付物一；8 个技能规格写入 | `agents/skills/S0`–`S7` 共 8 个 `.md` | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-009 | 2026-09-25 | 建立 Skills 索引并同步本总纲 | Skills 层索引化；目录约定改为实际结构；本总纲「当前状态 / 目录约定 / 对话索引」同步 | `agents/skills/README.md`、本文件 | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-010 | 2026-09-25 | 目录结构再调整；回填真实对话 ID | 交付物一改为 `agents/` 下并列 `agent`（Prompt）/ `skills` / `tools` / `workflow` / `evaluations`；对话 ID 经会话记录 `sessionId` 核定并回填 C-001–C-009 | `agents/skills/README.md`、本总纲 | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-011 | 2026-09-25 | 开放索引维护权限；设计 Expert 最小工具集合 | 索引转由 AI 长期维护、不再逐次征询；确立 Tool／Skill 分界判据，定出 6 个工具（文档解析 / 客观核查 / 引用解析 / 确定性计算 / 契约校验 / 资产库） | 对话内产出（工具集设计未落盘） | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-012 | 2026-09-25 | 确认 LearnBuddy 平台侧的工具调用方式 | 平台无「开发者注册工具」通道（官方明文：所有工具权限由系统统一分配）；确定性能力只能做成 CLI 脚本，由内置 `Bash` 调起；打包落点 `skills/<name>/scripts/` 或 `bin/`；插件根仅认 `agents`/`skills`/`bin`/`avatars`；`agent.md` 不得声明 `tools:` 字段 | 对话内产出（后整理为工作区 `工具使用说明.txt`） | `f46f58d9-d1da-43cb-9c96-232b387be86d` | [C-003–C-019](C-003-C-019.md) |
| C-013 | 2026-09-25 | 落盘 Tools 层：规格 + 脚本 + 规则集 | 6 个工具规格 T1–T6、Tools 索引、内置默认规则集（教师层按 `ruleId` 覆盖 / 追加，默认层只读）、6 个 python3 CLI 脚本（全部通过自测）；确立统一退出码 0/1/2/3（3 = 降级运行，CI 视为不通过） | `agents/tools/` 共 14 个文件 | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-014 | 2026-09-25 | 同步总纲；规格补脚本路径指向；确立「释放文件进程」协作规定 | 总纲第三 / 五 / 六节同步 Tools 层已落地；T1–T6 六份规格各增「脚本」路径指向行；新增协作规定：每轮对话结束前须释放产物与工作区内文件的进程，否则用户无法修改文件 | `agents/tools/T1`–`T6` 六份规格、本总纲 | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-015 | 2026-09-25 | 设计并暂存 System Prompt v0.1；提出四模式设计待评审 | Prompt 层首稿落盘（§0 身份 / §1 边界 / §2 流程 / §3 评分口径 / §4 证据 / §5 不确定性 / §6 教师复核 / §7 输出骨架 / §8 禁止事项 / §9 工具 / §10 降级）；用户提出四模式（建标 / 评阅 / 复核 / 教学分析）待定 | `agents/agent/SYSTEM_PROMPT.md` | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-016 | 2026-09-25 | 开放总纲非冻结区域编辑权限；四模式定稿并给出实现规划 | 编辑授权扩至全部状态性与事实性内容（三 / 五 / 六 / 七 / 八 / 九），此后自行记录、不再逐次征询、每次更新须在对话中说明；四模式按建议定稿（建标收窄为「样板检索 → 待确认稿」、评阅新增批量参数、复核定为只读回放、教学分析定为事实型聚合并拆出诊断）；输出实现规划：三模式落在已有能力，模式 4 需新增 T7 聚合器与 CohortReport 契约 | 本总纲 | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-017 | 2026-09-25 | 扩展 S7 以打通模式 3；分析模式 4 的要点与矛盾；模式 4 本期不实现 | S7 由「复核清单生成」扩为「复核与解释」，新增入口 B「解释回放」，含三条硬规则（只回放不重判 / 教师改过的项不解释原因 / 只读无后置）；标出两个外部要求 R1 读取既有 `ReviewResult` 的通道、R2 契约须逐项携带「建议分 + 终值」；模式 4 给出候选方案与影响分析后定为本期不实现 | `agents/skills/S7-review-checklist.md`、本总纲 | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-018 | 2026-09-25 | 更新 Skills 索引至四模式；补 SYSTEM_PROMPT §7.2 终值字段 | `skills/README.md` 第一节由「两条任务链」改为「四模式 + 生产组 / 消费组」，S0 行标注「四模式路由待扩展」，结构说明由「六段」纠正为「七段」并补多入口技能写法；`SYSTEM_PROMPT §7.2` 的 `items[]` 补 `score`（终值）与 `overriddenByTeacher`，`total.value` 标注按终值计算，§7.3 增两条：终值不由 Expert 填写、每项须同时保留建议分与终值。S7 的 R2 由此部分闭合（待契约冻结时对齐） | `agents/skills/README.md`、`agents/agent/SYSTEM_PROMPT.md` | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-019 | 2026-09-25 | 更新 S7 的 R2 现状；一次性批准在第十节登记「模式 4 本期不做」 | S7：R2「现状」由「缺终值字段」更正为「骨架已具备（`score` + `overriddenByTeacher`），待契约冻结对齐字段名」，并补「终值由谁写入」说明（`suggestedScore` 由 Expert 给，终值不由 Expert 填，本入口只读不写）；总纲第十节追加「模式 4 本期不做」一条（用户仅批准此次），并在顶部授权块登记该一次性例外 | `agents/skills/S7-review-checklist.md`、本总纲 | `01a9cc51-16b1-4f39-878d-a14859995d64` | [C-003–C-019](C-003-C-019.md) |
| C-020 | 2026-09-25 | 完整审查项目、评估进度、拟定修订列表 | 三份规划文件全文精读 + 交付物一 16 个规格/脚本逐份读取 + 6 个 CLI 脚本实跑取证。进度结论：交付物一约 50%、交付物二约 0%、交付物三约 5%，三交付物等权合计约 18%；唯一根阻塞为路径 0.1。输出 29 项分级修订列表（P0 阻断 6 / P1 正确性 5 / P2 一致性 14 / P3 可选 4）。报告落 `.work/`，本轮未改动任何交付物 | `.work/AutoGrader 项目审查报告_2026-09-25.md` | `172820c6-1a33-44d3-b351-96138b6ddf05` | [C-020–C-026](C-020-C-026.md) |
| C-021 | 2026-09-25 | 冻结 `ReviewResult` 契约（路径 0.1 + 0.3）并同步连带影响 | 按《契约冻结方案》D1–D5 建议执行。关键裁决：路线 A（以 v0.1 基线的向后兼容超集，新增字段一律 optional）；档位模型取 v0.1 `levels[]`、废弃 `bands[]{from,to}`（后者对真实 Rubric 恒为 `None`，档位检查静默跳过）；`needsReview` 与 `pending` 并存（合并会打爆 12 份资产总分）；T5 自行扩展 Schema 子集且「未知关键字即 exit 2」；`upperBound` 由 `100 − W_r` 改为 `Σ已计入 weight`（旧式在权重和 < 100 时给出偏大上界）。新增 `contract/`（两份 schema + Python/JS 双侧指纹实现）与 `docs/contract.md`；改造 T4/T5；同步 S5（三重校验 → 六项）、S7（R2 ✅ 闭合）、SYSTEM_PROMPT v0.2（§3.2 + §7.2）、tools README。验收全绿：12 份历史资产一行不改、同时通过 Zod（v0.1 前端契约）与 T5（工具链）双重校验 12/12 且 `skipped` 为空；指纹双侧复算 12/12 与声明值相等 | `contract/` 4 文件、`docs/contract.md`、`agents/tools/T4`、`T5`、`T3` 三份规格、`agents/skills/S5`、`S7`、`agents/agent/SYSTEM_PROMPT.md`、`agents/tools/README.md`、本总纲 | `172820c6-1a33-44d3-b351-96138b6ddf05` | [C-020–C-026](C-020-C-026.md) |
| C-022 | 2026-09-25 | （补录）模拟测试 Skills 与 Tools 能否正常使用 | 未形成结论——会话因网络错误中断（`502 安全连接建立失败`，TLS 在本地代理 `127.0.0.1:7890` 处被拦截），该请求从未被执行。此条登记的意义在于：它解释了该时段为何没有产出 | 无 | `50f4d488-430f-4552-9c46-3a16b57d73d1` | [C-020–C-026](C-020-C-026.md) |
| C-023 | 2026-09-25 | （补录）评估「评委在另一台机器一键拉起 LearnBuddy 并自动安装专家包」的可行性 | 链路串得通，但拿不到「一键全自动」——真实形态是 4–5 次点击，且硬前提是评委机器已装 LearnBuddy 桌面端（网页无法探测、无法代为安装）。专家包固定落点 `$WORKBUDDY_CONFIG_DIR/plugins/marketplaces/my-experts/plugins/<名>/`，不在该目录则无法被检测到；四条通道中只有内置 `expert-manager` 技能可由智能体自主驱动（深链 `skills=` 走平台自有市场、不支持任意 GitHub 仓库；「添加市场」需账号与后端；深链 `expertId` 首次用不了）。定论：正解是仓库内直接放已合规专家包，prompt 只做「复制 + 运行 register 脚本」 | 对话内产出（结论已进项目记忆与第八节） | `d00f9c2f-8145-46f8-a5fa-9046fcbe576a` | [C-020–C-026](C-020-C-026.md) |
| C-024 | 2026-09-25 | 按 AUDIT.md 推进正确性整轮：修 P0-1（T1 DOCX 标题识别全失）+ 第二步 6 项 | 以 `AUDIT.md`（33 条）为清单，按它 §8 的优先顺序执行「第一步② + 第二步」。P0-1 双根因实测确认：① 项目自己的两份 docx 442/233 段落全部无 `w:pStyle`，标题只由段落级 `w:outlineLvl` 标记（8+18=26、10+7=17 个标题）；② `handbook.docx` 的 `w:pStyle` 是数字 styleId，真名在 `styles.xml`（`1→heading 1`，9+16+3=28 段，其中 2 段无文字）。修法三处：`outlineLvl` 回退 + `styles.xml` 样式名映射 + 有正文无标题时写 `fail()` 降级 status；并给 T2 加「一个标题都没有时结构规则进 `notCovered`、绝不输出『缺失』」（L1）。另修：P1-7 收尾（T1/T2/T3/T6 入参形状防御；T3 静默放行——证据忠实度唯一硬闸门——已堵死）、P1-9（T4 rank 缺 confidence 不再静默默认 1.0）、P1-13（T2 自行复算并与 `summary` 交叉核对，不一致即 exit 2）、P1-15（S3 补 T3、S4 改 T4、S7 改 T4；Skills 统一改用 T1–T6 编号）、P1-16（S2 删除「支持截图」的错误承诺）、P2-17（T1 新增 `rawText` 逐字栏，T3 改用它）、P2-18/19（T3 摘要拆分 + 歧义报出 + 原因枚举对齐）、P2-22（T6「目录不存在」≠「库为空」）、P2-23（T6 `--id` 防越界 + 写入扩展名白名单）、P3-28/29/30/31/32/33。验收：六脚本自测 54 项全通过；三个真实 docx 26/17/26 标题；契约冻结回归 12/12 未破；指纹双侧 12/12；六脚本 × 非法输入 0 裸 traceback | `agents/tools/scripts/` 6 个脚本、T1–T6 规格、`S2`/`S3`/`S4`/`S5`/`S7`、`agents/tools/README.md`、本总纲 | `172820c6-1a33-44d3-b351-96138b6ddf05` | [C-020–C-026](C-020-C-026.md) |
| C-025 | 2026-09-25 | 排外模式 4；三项委托自行处理；出 docx 裁决清单 | ① 模式 4 整体排外：从设计面清除全部引用（`docs/contract.md` 的 `taskType` 行、`SYSTEM_PROMPT` 头部、`skills/README.md` 第一节与 S0 行、总纲第三节 Prompt 行与第五节）；「四模式」全部改为「三模式」（生产组 1 建标 / 2 评阅；消费组 3 复核）。用户自有文档（`AUDIT.md`、`契约冻结方案.md`）、历史索引行与第十节的排除留痕未动。② Workflow + Evaluation + 打包层按裁决另开对话构建，本轮不做。③ docx 过期部分摊成裁决清单（规划书 5 条 + 我补 4 条；目标定义书 4 条 + 我补 3 条）并附版本策略三选项，本回合未改任何 docx。④ 三项委托的结论：P1-11 定为「`skipped` 非空一律返 3」——依据 L1「未知优于否定」，校验没跑全时错误清单本身不可靠，不应作为判定依据；新增 `exitReason`（`ok` / `errors` / `degraded` / `errors-degraded`）与 `degraded` 字段，错误清单与跳过项始终逐条保留，信息不丢。P2-24 的 `total` 改为与契约一致的 2 位小数（消除「6dp→2dp 进位」造成的改档，档位线一律与报告值比较），未取整原值另存 `totalRaw`；`--grade-bands` 档位名改为与默认档位同序列（跳过 E，即 `A/B/C/D/F…`），并拒绝非数字 / 越界 / 重复 / 超量输入。P2-26 的「待批量参数」改写为「需新增契约字段 + 归属 Skill，当前未设计」，去掉「能力齐备只欠参数」的误导。⑤ 顺带发现并修复一处真缺陷：指纹算法要求 `provenance.resultFingerprint` 键必须存在（缺失则补 `""`）——「键不存在」与「键为空串」序列化成不同文本，生产者不补键、校验者补键时两侧复算必然不等 | `docs/contract.md`、`agents/agent/SYSTEM_PROMPT.md`、`agents/skills/README.md`、`agents/tools/scripts/deterministic_calculator.py`、`contract_validator.py`、`contract/fingerprint.py`、`contract/fingerprint.mjs`、`.work/docx 过期部分裁决清单_2026-09-25.md`、本总纲 | `172820c6-1a33-44d3-b351-96138b6ddf05` | [C-020–C-026](C-020-C-026.md) |
| C-026 | 2026-09-25 | 按裁决清单修订两份 docx，出 v1.1 + 口径变更说明 | 采用版本策略 C：v1.0 原样存档 + v1.1 修订版 + 口径变更说明，三份都交。链路：T1 解析 v1.0 docx → markdown（复用 T1 的结构判定）→ 逐条套用裁决建议 → `create_doc` + `doc_insert_markdown` + `save_file` 生成为 docx。规划书 11 处（1.1.3 判据由「手算」改为「T4 产出」；1.2 五项旧技能 → S0–S7 八项；1.3 四项工具 → T1–T6 六项；1.3.3「三项」→六项；1.3.2 加「待交付物二建立后校验」；1.3.1 补 `txt` 与 PDF 局限；新增 1.0 三模式总览；新增 1.7 打包与平台合规映射；0.1/0.3 事实性修正）。目标定义书 18 处（1.5 改名并删倒计时段；第五章「36 小时版」→「交付清单（无排期）」并删两条备用链接行动项；6.1 风险登记整节删除；6.2 待决策 →「已决事项」并填四项结论；4.1/4.2「三重校验」→双端六项；4.2 E3 补专家包侧；3.2 形态 A 改「部分构建」并删备用链接缺口行；第四章加「v0.1 资产待重建/待移植」说明；四处证据清单加标注）。验收：三份新 docx 经 T1 解析均 `status=ok`（19 / 25 / 7 个标题）；逐段差异比对得出规划书 11 段被改、20 段新增、目标定义书 28 段被改、29 段新增；v1.0 时间戳未变（确证原样存档）。超出裁决清单的 8 条连带/事实性修正已单列成表（含「原图实为文本框、无位图，文字已保留但未复刻版面」）。一处未改并保留：4.5 开头的「36 小时风险」措辞（属未点名部分，按「不碰只提」处理） | `AutoGrader 步骤路径规划书_v1.1.docx`、`AutoGrader 项目目标定义书_v1.1.docx`、`AutoGrader 口径变更说明_v1.0.docx`、本总纲 | `172820c6-1a33-44d3-b351-96138b6ddf05` | [C-020–C-026](C-020-C-026.md) |
| C-027 | 2026-09-25 | （补录前置会话）按设计使用流程审查项目、debug、修正引用 / README / docx 一致性并清理死代码 | 未形成结论——会话在首轮推理阶段被用户中断（该会话只引用了两份 v1.1 docx、未附总纲）。同一诉求随后在 C-028 重开并完成。登记意义：使 23:47 这个时间点有据可查，并解释该时段为何无产出 | 无 | `4d126b42-25e4-42e4-ad50-4b027ea84c35` | [C-027–C-027](C-027-C-027.md) |
| C-028 | 2026-09-26 | 按设计使用流程审查项目：修正各项引用 / README / 契约说明的一致性 + 清理死代码（docx 部分待裁决） | 先复跑六脚本自测取基线（61 项，总纲原写「54 项」已过时）→ 再端到端实跑 T1→T2→T3→T4→T5。已修：① 「三重校验」残留 6 处 → 六项校验（`skills/README`、`S1`、`SYSTEM_PROMPT §2`、`T5` 规格标题、`docs/contract.md` ×2、`contract_validator.py` docstring）；② S4 上界口径错（`[0, 100 − W_r]` → `[0, Σ已计入 weight]` / 上限 = 已计入权重合计），与 SYSTEM_PROMPT §3.2 / T4 规格 / `docs/contract.md` §5.3 对齐；③ T3 出参字段名与原因枚举（`resolved[].blockDigest` → `blockTextDigest`、补 `quoteDigest`、删脚本从未产出的 `anchor-out-of-range`、补 `block-ref-not-found`、`evidence[].quote` 来源由 `blocks[].text` 改为 `rawText`）；④ 死代码 3 处（T4 `WEIGHT_SEQUENCE` 常量、T1 `Builder._code_page` 属性、T5 `check_bands()` 的 `idx` 形参，均 grep 验证零引用）；⑤ 总纲 §三 自测项数、§六 git 事实与目录树。端到端实跑查出 1 处 P0 级契约冲突（未改，待裁决）：T4 满权重时产出 `total.grade = "A"/"B"/"C"/"D"/"F"`，而冻结的 `ReviewResult.schema.json` 把 `total.grade` 定义为 `ScoreLevel`（excellent/meeting/partial/notMet）或 `null` → T5 报 `schema.oneOf @ $.total.grade`、退出码 1；实测同一结果把 `grade` 置 `null` 则六项全过（exit 0）。另列 docx 3 处、S6 悬空行、`工具使用说明.md`/`err5.txt` 等入裁决清单。续（同日 00:20 起，按用户裁决执行）：① S6 悬空行已删（「（批量时）班级级共性问题」）；② T4 / T5 的 `--out` 已补 try/`OSError` → 结构化报错 + 退出码 2（实测两脚本均 `kind=output-error`、无裸 traceback，与 T1/T2/T3/T6 对齐）；③ 删 0 字节的 `err5.txt`；④ `contract_validator.py` 增 `sys.dont_write_bytecode = True`（实测 T5 全流程跑完不再生成 `__pycache__`，含它 importlib 加载 `contract/fingerprint.py` 那一路）；⑤ 新增根 `README.md`（开发期版本，写实、7 条未闭合项如实列出；README 内记录的每条命令均已实跑验证）。docx 裁决已下达并执行：「§4.1 用 A1–A5」经回报与同文档 §4.3 已有的 A1–A5 在同一章撞名后，用户改裁 §4.1 用 I1–I5；另裁「§4.5 的 36 小时风险一并改为范围失控风险」「《口径变更说明》不碰」。目标定义书 v1.1 改 6 处（§4.1 关键举措 T1–T5 → I1–I5、§4.5 措辞）、规划书 v1.1 改 6 处（§1.6 表首列手段序号 S1–S6 → M1–M6），均就地修订、不升版本号（旧版备份在 `.work/backup_v1.1/`）。「同步修改引用」为空操作：grep 确认两处编号无任何跨处引用。验收：回读判据 17/17；逐块比对两份各恰 6 块不同、块数 `205→205` / `125→125`、标题数 25 / 19 不变 → 整文重生成无连带变化；v1.0 原稿未动。未做：《口径变更说明》§五 写着「规划书 1.6 未改」，改后成新缺口，已列入待确认。→ 01:00 起：P0 契约冲突已修——`total.grade` 由 `ScoreLevel` 枚举放宽为「等级名或 `null`」，契约 1.1.0 → 1.2.0（放宽取值域 = MINOR），同步：2 份 schema（`$id` + 新增 `$defs.Grade`）、`docs/contract.md`（标题/§二 增「放宽取值域→MINOR」/ §5.4 新增取值域表/§十二）、9 处版本号引用（`SYSTEM_PROMPT` / `tools/README` / `T5` 规格 / `T6` 规格 / `T4` 脚本 / `contract_validator.py` 夹具 / `README.md` / 本总纲）。验收：六脚本自测 61 项；当初失败的用例（`grade='A'`）T5 exit=0 六项全 pass；12 份历史资产回归 12/12（T5 六项 + 指纹复算双双通过，用随包契约未传 `--schema`）；负向对照「`123` / `true` / `{}` 一律被拒」证明未放宽成不校验。指纹模块自身的 `version: 1.1.0` 有意保留（那是模块版本，算法未动）。→ 01:20 起：R1 ✅ 闭合（用户裁决：走平台内置读取能力，不扩展 T6；通道写进 `S7` ×5 处、`docs/contract.md` §十三、`SYSTEM_PROMPT` §9、README、本总纲 §五）；12 份历史资产的 `total` 已回填——落点 `web/public/results/`（总纲 §六 规划指定给「评阅结果 JSON 资产」处），不落 v0.1：实测 v0.1 的 Zod 顶层是 `.strict()` 且无 `total`，加键后 12/12 全部判不合格，且其前端 `lib/data.ts` 读资产时调 `validateReviewResult` → 会让 `/eval`、`/report/[id]` 页降级。每份仅新增 `total` 块（7 行）+ 重算 `resultFingerprint`（1 行），不动任何已有字段、行尾保持 LF。⚠️ 本轮踩到的易漏点：指纹覆盖整个对象，加键后不重算指纹必报 `fingerprint.mismatch`（首轮 12/12 全挂）——`docs/contract.md` §12 规则 4 原文只写「推导 `total`」，已在新增的规则 5 补齐。验收：新落点 T5 六项全过 12/12 且 `skipped` 为空、指纹自洽 12/12；v0.1 原件 Zod 12/12 未受影响、`git status` 为空 | `agents/` 9 个文件（`SYSTEM_PROMPT`、`skills/README`、`S1`、`S4`、`T3`/`T5` 规格、`contract_validator.py`、`deterministic_calculator.py`、`document_parser.py`）+ `docs/contract.md`、`contract/ReviewResult.schema.json`、`contract/Rubric.schema.json`、`tools/README.md`、`T6-asset-store.md`、`S6-feedback-writer.md`、`README.md`、`AutoGrader 项目目标定义书_v1.1.docx`、`AutoGrader 步骤路径规划书_v1.1.docx`、本总纲（`err5.txt` 已删） | `48af976b-0cf0-4375-aec4-e245e93412a1` | [C-028–C-033](C-028-C-033.md) |
| C-029 | 2026-09-26 | 解冻 Workflow / Evaluation 两层，执行《规划书》实现步骤 1.4、1.5 | 用户裁决：「解冻 workflow、evaluations 层，执行实现步骤 1.4、1.5」。① Workflow 层（1.4）：`agents/workflow/` 落 `README.md` 索引 + W1 五 Agent 编排规格（逐环四栏「输入 / 输出 / 置信度 / 证据引用」+ 环间交接物表 + 每环硬闸门 + 契约落点 `steps[].agent` 五值；并说明 S7 不属这五环）+ W2 编排形态（单会话分步 · 多会话分环；共同硬约束 6 条 + 选型判据 + 等价性 4 条判据，显式标注「两形态实跑对照须在平台侧对话完成，本工作区只保证规格齐备与产物形状可校验」）+ W3 失败与重试（原则 F1–F3 + 8 个状态标记闭环词汇表 + 12 类失败处置 + 回退边界表 + 六脚本退出码与 CI 判定映射）。② Evaluation 层（1.5）：`agents/evaluations/` 落 `README.md` + `metrics.md`（指标口径真源，只在此定义一次） + `scripts/evaluate.py`（自测 17 项）+ `scripts/regression_gate.py`（自测 10 项）+ `baselines/2026-09-26.json` 与 `.report.md`。四项指标 = 总分 MAE / 逐项命中率 / 档位一致率 / 置信度校准（分桶阈值直接取契约 §八的 0.60 / 0.80；ECE 用已展示的 2 位小数加权，以换取「读者可拿计算器手工复算」）；另按项目已公开的「差档 MAE」补了 MAE 的 `byTier` 分组视图（档序取金标准中首次出现的次序，数据驱动）。实测（12 份资产 × v0.1 金标准）：总分 MAE 3.05、差档 MAE 8.0、单份最大绝对偏差 14.0（`sample-04`） —— 三项与 v0.1 已公布数值逐值一致（独立复现 → 证明口径同源、不是另起一套算法）；档位一致率 0.74（106/144）、置信度校准 ECE 0.12。门禁三态实测：基线对自己 → 0；人为劣化三项（MAE 3.05→3.50、一致率 0.74→0.70、ECE 0.12→0.20）→ 1（三项均标 `REGRESSED`）；`--tolerance 0.5` → 0（波动被吸收）。③ 同步：总纲 §三（两层状态改 ✅）、§五（路径一 → 只剩打包层）、§六（`documents/` 移入 + 两层内容）、§九（本行）。一处事实校正：v0.1 README 写「差档 4 份」，而其金标准 manifest 中 `tier = 差` 实为 3 份（sample-04 / 08 / 12）——份数以金标准数据为准，MAE 数值不受影响（两处均为 8.00） | `agents/workflow/` 4 文件、`agents/evaluations/` 6 文件、本总纲 | `48af976b-0cf0-4375-aec4-e245e93412a1` | [C-028–C-033](C-028-C-033.md) |
| C-030 | 2026-09-26 | 建随包资产根；解冻打包层，装配专家包并注册为可召唤的本地专家 | 用户裁决：① 建立随包资产根；② 解冻打包层，参照附件 `frontend-design-expert.zip` 做映射并补齐文件，把专家包做成可在 LearnBuddy 召唤的本地专家。① 随包资产根：`agents/tools/assets/` 四类落定 —— `template/` 1 份通用 Rubric 样板、`rubric/` 空（待教师写入）、`sample/` 12 份样例报告、`gold/` 1 份金标准 manifest；内容全部来自 v0.1 `demo/`，SHA-256 逐字节核对一致；另附 `assets/README.md` 记录四类用途、「`rubric` 空目录返回退出码 1」的正确语义、两条已知限制。实测 T6：template / sample / gold 各为 1 / 12 / 1 项 exit 0，`rubric` 0 项 exit 1（未命中，正确语义）。② 打包层：新增 `backend/autograder-expert/build_expert.py`（自测 21 项）+ `packaging/expert.manifest.json`（映射唯一真源）。关键映射裁决：平台插件根只认 `agents` / `skills` / `bin` / `avatars`，没有 workflow / evaluation / tools 槽位 → 8 个技能映射为 8 个 `skills/<名>/SKILL.md`；Workflow 与 Evaluation 各映射为一个「规格技能」（`SKILL.md` + `references/`）；Tools 与两个评测 CLI 合并进 `skills/autograder-tools/`（`scripts/` + `rules/` + `contract/` + `assets/` 同级）—— 如此 T2 的 `../rules`、T6 的 `../assets`、T4/T5 的向上查找 `contract/` 三处路径解析一行代码都不用改；Prompt 映射为 `agents/autograder-expert.md`（按该文件自己的说明剥离头部状态块与文末附录）。官方链路实跑：`init_expert.py` 生成骨架 → 装配 54 个文件 → `validate_expert.py` ✅ Expert package is valid! → `register_expert.py` 已注册（`marketplace.json` 中原有的 `frontend-design-expert` 被保留、未被覆盖）→ `package_expert.py` 打出 52 文件 / 197.9 KB 的 zip。包内验收（本轮关键证据）：从包内位置跑全部 8 个 CLI 自测 88 项全通过；T6 从包内找到样板；T5 从包内自解析到 `skills/autograder-tools/contract/ReviewResult.schema.json` 且 `valid=True`。已知未做：头像（`avatars/` 为空）、两种编排形态的实跑对照（需真实会话）。→ 02:56 补记（用户令「生成头像」）：实测确认本环境不可用图像生成工具（`ToolSearch` 精确名无匹配；关键词检索只出 `VideoGen`/Ardot；`DeferExecuteTool` 直呼 `ImageGen` 返回 `not available in the current environment or configuration`）→ 按官方头像规范的「生成失败处理」条款，把推荐 prompt（含逐项提取依据）与两种补法写进 `packaging/PLUGIN_README.md` §六，并重新装配使包内 README 同步。同轮修掉装配器一个破坏性缺陷：原「先整体删目录再写入」被宿主「单轮删除 >50 项需确认」的安全闸打断在半途、把包打坏；改「增量删除」后又误删 24 个目录（被自测 `pruneKeepsDeclared` 抓到）→ 现为 `stale_items()` 只报告不删除，要删须显式 `--prune`。恢复验收：写盘 54 文件、无残留、官方校验 valid、已注册；包内 8 个 CLI 自测 88 项全过；T5 从包内自解析契约 `valid=True`；Python 复核 54 个交付文件且清单与落盘零差异（装配刚跑完的瞬间另有 `.created-by-session`，该标记稍后被外部清掉 —— 参考包 `frontend-design-expert` 同样没有它，属宿主对市场目录的同步行为，非缺陷；判断包完整性的口径就是这 54 个文件） | `agents/tools/assets/`（15 文件）、`build_expert.py`、`packaging/`（manifest + PLUGIN_README + dist/autograder-expert.zip）、本总纲 | `48af976b-0cf0-4375-aec4-e245e93412a1` | [C-028–C-033](C-028-C-033.md) |
| C-031 | 2026-09-26 | 生成头像的尝试 + 修掉装配器的破坏性缺陷 | ① 头像：按官方头像规范从 Agent MD 提取特征构建了 prompt（背景色调取 `10-ProjectQuality` 规定的 green-emerald），但本环境不可用图像生成工具（精确名查找无匹配 → 关键词检索只出 `VideoGen`/Ardot → `DeferExecuteTool` 直呼 `ImageGen` 返回 `not available in the current environment or configuration`）→ 按规范的「生成失败处理」条款，把推荐 prompt（含逐项提取依据）与两种补法写进 `packaging/PLUGIN_README.md` §六 并重新装配入包。未产生额度消耗；也未用几何图形充数。② 装配器缺陷：原「先整体删目录再写入」被宿主「单轮删除 >50 项需确认」的安全闸打断在半途、打坏了包；改「增量删除」后又误删 24 个目录（被自测 `pruneKeepsDeclared` 抓到）→ 最终改为 `stale_items()` 只报告不删除（要删须 `--prune`），并在 docstring 写下理由：装配器不该具备破坏性。③ 更正一处我自己的错误判断：先前把「会话标记反复消失」解释为「bash 路径读数不可靠」是错的 —— 实为时间差（写入时在、稍后被外部清掉；参考包同样没有该文件）。包完整性口径 = 54 个交付文件。恢复验收：官方校验 valid、已注册、包内 88 项自测全过 | `build_expert.py`、`packaging/PLUGIN_README.md`、本总纲 | `48af976b-0cf0-4375-aec4-e245e93412a1` | [C-028–C-033](C-028-C-033.md) |
| C-032 | 2026-09-26 | 按规划书核对 1.6 之前的步骤是否实现且自检通过 | 判据逐条取自 docx 原文（T1 解析 status=ok / 19 标题），范围 = 路径 0（0.1–0.6）+ 路径一 1.0–1.5，共 32 条。结论：32 条全部有结论，未通过 0 条 —— PASS 17（可脚本复算且实测通过）/ SPEC-ONLY 14（Prompt 与 Skill 的行为类判据，须真实会话验）/ DEFERRED 1（1.3.2 的跨端一致性，判据原文自明「待交付物二建立后校验」）。关键证据：契约校验双份 12/12；指纹 Python/JS 双侧 12/12；对话索引 31 条全部可定位（C-012 按记载在另一工作区目录，已核实）；含待复核项时 `upperBound == weightIncluded == 60`（旧式会算 100）；「目录不存在 exit 2」与「库为空 exit 1」可区分。两处我自己的探针误判已更正（要求纯文本必须 `status=ok`；用白名单内的 `definitions` 去测「不支持的关键字」，应改用 `uniqueItems`）。一处真缺口：判据要求四类输入可解析，而本机无 PDF 样本 → PDF 分支未验（缺样本非缺实现，已显式标注）。核对台 `.work/audit_plan_pre_1_6.py`；报告 `.work/规划书1.6前核对_2026-09-26.md` | `.work/规划书1.6前核对_2026-09-26.md`、`.work/audit_plan_pre_1_6.py`、本总纲 §五（路径 0 → 五项全部冻结完成） | `48af976b-0cf0-4375-aec4-e245e93412a1` | [C-028–C-033](C-028-C-033.md) |
| C-033 | 2026-09-26 | 1.6 与 1.7 一并审查 | 范围扩到 1.6（输出稳定性专项 M1–M6）+ 1.7（打包与平台合规映射：6 条硬约束 + 明写的四步完成判据），共 16 条。结论：16 条全部有结论，未通过 0 条 —— PASS 13 / SPEC-ONLY 2（1.7 判据③「专家中心人眼可见」、判据④「给它一份新报告能产出合规 JSON」，均须真实会话或 UI）/ DEFERRED 1。关键证据：M1 塞未定义字段 → T5 报 `schema.additionalProperties`；M2 档位改成枚举外的 `good` → T5 报 `band.level-not-in-rubric`（少自由度确实被契约钉死）；M4 校验失败时 `errors[]` 每项带 `fixHint`（可驱动重试）+ W3/S5 写明「限定轮次 → 待人工」；M5 用 14 组语义层畸形输入（空文件 / 超长 2.7MB / 二进制垃圾 / 无文字层 docx / 路径不存在 / 缺字段 JSON / 越权写入）实测全部结构化返回、裸 traceback 0 例，并另测语法层（flag 拼错）六脚本一致 exit=2；1.7 六条硬约束全部实测通过（脚本落 `skills/…/scripts/` 首选路径、包内 11/11 技能目录含 `SKILL.md`、frontmatter 无 `tools:`、落点正确且已注册）。一处实质缺口：M6 的「对抗样本集」尚未单列（`assets/` 四类里没有对抗样本目录；素材需新造，不能靠复制 v0.1 的 12 份样例补齐）。又更正两处我自己的断言写错（拿英文 Agent 名断言中文分步的 Prompt；按字面找带反引号的措辞）。核对台 `.work/audit_plan_1_6_1_7.py`。全貌：1.6 之前 + 1.6 + 1.7 合计 48 条判据，FAIL 0；待平台侧实跑 16 条；缺口 2 处（对抗样本集、PDF 样本） | `.work/audit_plan_1_6_1_7.py`、`.work/规划书1.6前核对_2026-09-26.md`（§六）、本总纲 | `48af976b-0cf0-4375-aec4-e245e93412a1` | [C-028–C-033](C-028-C-033.md) |
| C-034 | 2026-09-26 | 交付物二（Web Demo）开工：按规划书路径二 2.1–2.8 制定计划并执行第①段；本会话多次被网络中断 | 用户 13 项裁决（计划存 `~/.learnbuddy/plans/stellar-thunder-turing.md`）：复用 v0.1 骨架 + 重做视觉 / 2.5 含 docx 浏览器端解析 / 沿用 GitHub Actions → Pages / Zod 移植 + 构建期一致性检查 / 7 个路由 / 零依赖 SVG 自绘 / 视觉模仿 `LBEILC/RhineLabUI`（已取其 `DESIGN.md` token）/ 引入 MiSans 分包 / 单强调色 + 灰阶（数据语义不用红绿） / 仅微过渡 + 白场入场 + 460ms 编号滚动 / 分 3 段交付 / 仓库根 = 整个工作区 / workflow + `.gitignore` 都写好。；✅ 已落地：`web/` 骨架（Next 15.1.12 静态导出、basePath 默认空——修掉 v0.1「构建用子路径、服务用根路径」的老问题）；契约 1.2.0 Zod 补齐；三道构建期门禁（`sync-assets.mjs` / `contract-check.mjs` / `check-fonts.mjs`）；数据层改读 `web/public/assets/`（自包含）；MiSans 子集 113 MB → 四档 5.31 MB（`sync_fonts.py`，口径 = GB2312 ∪ 站点内容 ∪ 常用标点；修掉上游包两处缺陷：`font-weight` 全写成 400 且四档家族名各异、`src` 带 `local()` 会导致本机字体覆盖固定版本）；视觉重做（v0.1 约 94 处硬编码调色板已全部改写为 token + 灰阶 + 刻度符号）；7 个路由、2.3 `/agents` 五件套页、2.5 `/upload`（浏览器端零依赖 docx + 复刻 T2 的 13 条规则）；根 `.gitignore` + `.github/workflows/deploy.yml` + `web/README.md` + `fonts/PROVENANCE.md`。；✅ 已实测：`tsc` 0 错；契约门禁 23 对象层 / 132 字段零差异 + `$defs` 无静默漏检 + 12/12 资产 + 1/1 夹具 + 指纹双实现 16/16 逐字相同 + 12/12 声明指纹 == 复算值；字体门禁全绿；跨端对拍 12/12；`next build` 编译 ✓ / 类型 ✓ / 静态页 21/21 ✓。；⬜ 未闭合：2.6 深链入口未做；2.7 仍为构建期计算且缺档位一致率与 ECE、无 SVG 自绘；2.4 的指纹现场复算与 `total` 展示未接入；本机产不出 `out/`（宿主 safe-delete，见 §八）；线上链接待建仓。；判据影响：规划书 1.3.2 由 DEFERRED → PASS。；两处我要认领的问题：① 一次自我回归——为移植页面用 `cp` 覆盖了 `web/app/` 的 5 个页面，而此前被打断的轮次已适配过它们（`getContractExample→getTraceExample`、`variant="success"→"accent"`），`tsc` 报 5 处错后已逐处改回；教训：多轮中断的会话里动手前先按 mtime 盘目标目录。② 契约门禁抓出真缺陷——跨文件 `$ref` 丢了「当前文档根」（Rubric 文档内的 `#/$defs/RubricItem` 被拿去 ReviewResult 里找），已修为把 `docRoot` 一路带下去。；环境结论（详见 §八）：宿主 safe-delete 拦截器使「≥50 项批量删除」与「回收站二进制」两处失败；用户裁决「批量删除提权审批」→ 实测提权能过批量闸、过不了回收站失败，故这不是权限问题；未采用调阈值 / 清 `NODE_OPTIONS` 等绕过手段。 | `web/`（骨架 + 7 路由 + 4 新组件 + 15 个 lib 模块 + 6 个 scripts + MiSans 子集 227 文件 + 28 个数据资产）、根 `.gitignore`、`.github/workflows/deploy.yml`、本总纲 §五/§六/§八 | `faf17789-2885-49e6-ac87-2b3b8d4f03bc` | [C-034–C-038](C-034-C-038.md) |
| C-035 | 2026-09-26 | 按用户点名顺序补 2.6 → 2.4 → 2.7，并修复本地 `node_modules/.bin` 为空（同一会话的后续阶段） | ① `node_modules/.bin` 修复：此前 `npm install` 被宿主 safe-delete 打断，`.bin` 里只剩 75 个 npm 原子写临时文件（`.next.cmd-CJHbRNaw` 一类）、真 shim 一个都没生成 → 从 v0.1 复制其 `.bin`（版本逐包一致，且 shim 全是 `%~dp0\..\<pkg>\…` 相对路径、无绝对路径）。实测 `next --version` = 15.1.12、`tsc --version` = 5.7.3、`npm run typecheck` 可正常解析；3 个残留临时文件已清（钩子报错但实际删除成功）。；② 2.6 深链入口：新增 `components/deep-link-button.tsx` —— 顶层真实 `<a href="learnbuddy://task?action=start&prompt=…">`（不用 `location.href`）；点击后 1200ms 内监听 `visibilitychange`/`blur`，页面仍可见即判未装客户端 → `navigator.clipboard.writeText`（非安全上下文退 `execCommand`）+「再试深链」+ `<details>` 手动复制全文 + 兜底 `Link` 到 `/report/[id]`，任何分支都有可见结果。关键判断：`SYSTEM_PROMPT.md` 有 1.6 万字节、深链 prompt 上限 8000 字符，全文内联不可行 → prompt 只带「目标 + 任务 + 提示词 raw 地址」；`REPO_RAW_BASE` 留空（仓库尚未建，不编造仓库名），运行时按 GitHub Pages 站点推导 `raw.githubusercontent.com/<owner>/<repo>/HEAD`，推不出则降级为「向用户索取提示词」。；③ 2.4 补接入：结果页接 `getFingerprintCheck(report.id)`（页面本为 async）→ `ProvenanceBlock` 显示「复算一致」徽标 + 复算值 + 说明；新增 `total` 口径面板（已计入/未计入权重、上界、是否部分分、结果声明等级、未计入项，并原样排出 `TOTAL_FORMULA` / `UPPER_BOUND_NOTE` / `PARTIAL_SCORE_NOTE`）。；④ 2.7 补两指标：`lib/analysis.ts` 新增 `CONF_BUCKETS` / `buildLevelPairs` / `computeLevelAgreement` / `computeCalibration` + 两段口径文案，逐条照抄 `evaluate.py` 的 `comparable_pairs` / `compute_item_hits` / `compute_calibration`（含「可比对」定义、「一致率是汇总值」、「ECE 用已展示 2 位小数加权」三条易错点）。`/eval` 新增「置信度校准（ECE）」整区：ECE 值 + 分桶表（n / 平均置信度 / 实际命中率 / 校准差 / 加权项）+ 可手工复算的加权展开式。顺手纠一处标签错：原 StatCard 名为「逐项档位命中率」，但其值是 Σhits/Σcomparable 的汇总、与「逐项」不是一回事 → 正名为档位一致率，逐项值仍在「逐项命中率」页签内。另加两条构造路径的交叉校验：`itemHits` 与 `levelPairs` 的 Σ 不等即 `throw`（指标页出现自相矛盾的数字比没有该指标更糟）。；⑤ 验收（全部实测）：`tsc --noEmit` 0 错；四道门禁全绿（sync-assets / contract-check 23 层 132 字段 / check-fonts / 跨端对拍 12/12）；2.7 两指标与工具链基线逐值相等 —— 档位一致率 0.74（106/144）、ECE 0.12、三个桶 n/平均置信度/命中率/校准差逐值全同（脚本 `.work/verify_2_7.mjs`，刻意不经 `data.ts` 而直接读资产）；渲染核验 11/11（对预渲染 HTML 断言深链 href、按钮文案、`total` 面板、指纹复算结论、ECE 数值与展开式；深链 prompt 解码 162 字符 < 8000 上限；脚本 `.work/check_render.mjs`）。；⑥ 我要认领的一处回归：10:46 为移植页面执行的 `cp` 覆盖了此前轮次已换肤的 5 个页面，导致 54 处硬编码调色板（amber/emerald/rose/sky/slate）残留在 `app/{page,grade,eval,trace,report/[id]}`，违反已裁决的「单强调色 + 灰阶、不用红绿」；上一轮我只修了它引发的 5 处类型错误，直到本轮才检出视觉层损失。按「改动必须点名」纪律未擅自修，已列入待确认并以最高优先级呈报；本总纲 §五 原先写的「94 处已全部改写」据此已更正为不实描述。；⑦ 同步：本总纲 §五（路径二整行重写：2.1–2.7 落地 + 已实测 + 未闭合五项，含 2.7「构建期计算而非浏览器端现算」的口径偏差与调色板残留）、§六（组件新增项 + 落地差异第④条）。 | `web/components/deep-link-button.tsx`、`web/lib/constants.ts`、`web/lib/analysis.ts`、`web/app/eval/page.tsx`、`web/app/report/[id]/page.tsx`、`web/node_modules/.bin/`（本地工具链，不入库）、`.work/verify_2_7.mjs`、`.work/check_render.mjs`、`.work/preview-server.mjs`、本总纲 | `faf17789-2885-49e6-ac87-2b3b8d4f03bc` | [C-034–C-038](C-034-C-038.md) |
| C-036 | 2026-09-26 | 用户点名三项：调色板改 token、`/eval` 改浏览器端现算、实现零依赖 SVG 图表（同一会话的后续阶段） | ① 调色板清零：63 处 → 0 处。⚠️ 上轮报的「54 处」用了窄正则、漏了 `-50/-700/-800/-900` 深浅阶，完整口径实为 63。修法不是逐处替换，而是先立唯一来源：`lib/constants.ts` 新增 `TONE`（ok/notice/bad 三档，与 `badge.tsx` 的 accent/warning/danger 逐字一致）+ `deltaTone(abs, okMax, noticeMax)` / `ratioTone(ratio, okMin, noticeMin)`（阈值留在调用处，因不同指标量纲不同）；`trace/page.tsx` 的 `GRADE_CHIP`/`GRADE_BAR` 六条本地 emerald/amber/rose 色表删除，改走 `CONFIDENCE_GRADE_META`（`●●●/●●○/●○○` 三格刻度 + token chip/bar，并把符号显示出来）。另扫全仓其它 Tailwind 颜色工具类（gray/zinc/blue/violet…）→ 0 处。；② `/eval` 改为浏览器端现算：`app/eval/page.tsx` 退为薄服务端页（只读原始数据：金标准逐项档位 / AI 逐项档位与置信度 / rubric 权重），指标全部由 `components/eval-metrics.tsx`（`use client`）在运行时现算（`useEvalMetrics` 一个入口，`useMemo` 缓存，四个面板共用）；两条构造路径的交叉校验保留（`itemHits` 与 `levelPairs` 的 Σ 不等即 throw）。输入契约 `EvalMetricsInput` 全字段可 JSON 序列化；`resultStatus` 用 `'ok' | 'missing' | `'invalid'` 联合类型以对齐 `EvalRow.status`。<br>**③ 4 张零依赖 SVG 图表**（`components/charts/`，纯函数组件、`fill/stroke=currentColor`、不写死颜色）：`deviation-chart`（0 轴左右伸出 + ±2 容差带，带内浅/带外深）/ `score-comparison-chart`（**哑铃图**：空心=金标准、实心=AI，**线段即差距**）/ `calibration-chart`（可靠性图：对角线=说到做到，**实线=偏自信 / 虚线=偏保守**，点大小 ∝ 样本数）/ `hit-rate-chart`（横条 + **70%/90% 两道阈值参考线**，用参考线替代染色）。<br>**④ 一处超出点名的结构改动（已提出）**：`/eval` 由「页签」改为「分区堆叠 + 锚点导航」。**实测依据**：静态导出下页签只渲染当前那个，**非活动页签内容根本不进 HTML**（12 个 `<svg>` 全是图标、`role="img"` 计 0），图表会被藏在客户端状态后面、无 JS 时整块缺失；改为 5 个分区后实测 5/5 命中、`role="img"` 计 4。<br>**⑤ 验收**：`tsc` 0 错；四道门禁全绿（含字体门禁 **C1 源码文案 926 个非 ASCII 码位全部覆盖**——新增中文文案都在子集内）；`next build` 编译 ✓ / 类型检查 ✓ / 21/21 预渲染 ✓；`.work/verify_2_7.mjs` 通过（一致率 0.74 = 106/144、ECE 0.12、三桶逐值相等，**重构后未漂移**）；`.work/check_render.mjs` 通过（17 项断言 + 调色板清零 9 路由 0 处 + 9 路由 200 + 深链 prompt 162 字符 + 4 张 SVG 图）。` | [C-034–C-038](C-034-C-038.md) |
| C-037 | 2026-09-26 | 用户点名：注释性语句删减或收入二级菜单，保持页面文字简洁（同一会话后续阶段；`/eval` 与 `/trace` 的既有修改保留） | ① 手段：新增 `components/note.tsx`，用原生 `<details>`（不是 Tabs、不是 JS 弹层）。为什么必须是 `<details>`：本项目是 `output:'export'` 静态站，页签只渲染活动面板、非活动内容根本不进 HTML（`/eval` 刚踩过，见 C-036 ④）；`<details>` 收起时正文仍在 HTML 里 —— 可检索、可核验、无 JS 也能展开，故「收进二级菜单」不会退化成「把内容拿掉」。另导出 `Footnote`（一行式脚注，短且属于结论本身的不藏）。；② ⚠️ 新踩的坑（已记入日志，别再犯）：初版折叠标记用文字字形 `▸` U+25B8 → `check:fonts` 的 C1 报「1 个可能被渲染的码位未覆盖」。根因：MiSans 子集按源码实际用到的码位裁（`sync:fonts`），自敲几何符号落在子集外、真机显示豆腐块。正解：折叠/箭头标记一律用 `lucide-react` 图标（SVG 不进字体子集）；改 `ChevronRight` 后 C1 回到 926 码位全覆盖、零警告。；③ 改动面（10 个文件）：`app/page.tsx`（Compile-time AI 范式 / 虚线框含义）· `app/grade/page.tsx`（状态判定规则）· `app/eval/page.tsx`（浏览器现算声明）· `app/agents/page.tsx`（抽取源 / 退出码 0–3 / 为什么不用红绿）· `app/upload/page.tsx`（文件不出本机；能力边界条目瘦身）· `app/report/[id]/page.tsx`（脱敏说明 / total 口径细节 / 分数自证 / 不渲染不合格结果的理由 / 金标准基线用法 / 扫描规则）· `app/trace/page.tsx`（复核环节 / 数据来源）· `components/eval-metrics.tsx`（四图「怎么读」/ ECE 怎么算 / 命中率怎么定 / 六条口径定义）· `components/inspection-panel.tsx`（核查数据来源与边界）· `components/upload-inspector.tsx`（本地计算 / 摘要用途 / 「不适用」vs「不通过」/ 相似度不构成结论）；`lib/constants.ts` 新增 `TOTAL_FORMULA_LABELS`（total 口径表四个字段标签，页面与文档共用一套措辞）。；④ 🟠 一处 P1 事实错误（本轮自查发现并已修，非用户点名）：`app/trace/page.tsx` 的页头注释与页面正文均写「溯源链来自 `public/results/_example.json`（契约示例，非真实评阅结果）」，与实现不符 —— `lib/data.ts` 的 `getTraceExample()` 取的是首份通过契约校验的真实结果。已按实际重写（页头注明更正日期；正文改为「取自 `result-{reportId}.json`，与 `/eval` 同一份真实结果」）。同处一并纠出字段名错：`example.report.id` 应为 `example.report.reportId`（形状为 `{title, reportId, course, studentCode}`）—— 这正是 `tsc` 报出的那条错，说明该行此前从未被类型检查覆盖过。；⑤ 验收（全实测）：`tsc --noEmit` 0 错；四道门禁全绿（`check:contract` 6 项含资产 12/12 + 指纹 16/16 · `check:fonts` C1 926 码位零警告 · `audit:cross-end` 12/12 逐条一致）；`next build` 编译 ✓ / 类型 ✓ / 21/21 预渲染 ✓（仍卡 `.next/export` 清理，环境性、非本轮引入）；`.work/verify_2_7.mjs` 通过；`.work/check_render.mjs` 断言从 17 项扩到 35 项、全通过 —— 新增 18 条二级菜单断言专门验「折叠区正文仍在静态 HTML 里」，另加调色板清零 9 路由 0 处 / 9 路由 200 / 深链 prompt 162 字符 / 4 张 SVG 图。；⑥ 断言写法的教训（已写进脚本注释）：二级菜单断言只能覆盖当前构建下真的会渲染的分支。`/report/[id]` 的「金标准分是怎么来的」「这条基线怎么用」位于 `state.status !== 'ok'` 的降级分支，而 12 份样例当前全部有结果（v0.1 已回填 `total`），该分支不渲染 —— 我一度把它写进断言，得到一条永远不可能变绿的红灯，已删并注明原因：断言一个永远不出现的东西，只会伪装成绿灯或永久红灯，两者都在骗人。 | `web/components/note.tsx`（新增）、`web/app/{page,grade,eval,agents,upload,trace}/page.tsx`、`web/app/report/[id]/page.tsx`、`web/components/{eval-metrics,inspection-panel,upload-inspector}.tsx`、`web/lib/constants.ts`、`.work/check_render.mjs`、本总纲 | `faf17789-2885-49e6-ac87-2b3b8d4f03bc` | [C-034–C-038](C-034-C-038.md) |
| C-038 | 2026-09-26 | 用户点名：`/trace` 的 `ProvenanceBlock` 接入指纹复算（该页原为同步组件，接入需改 async） | ① 改动极小但性质明确：`app/trace/page.tsx` 由 `export default function TracePage()` 改为 `export default async function TracePage()`；新增 `const fingerprintCheck = example === null ? null : await getFingerprintCheck(example.report.reportId);`；渲染处补传 `fingerprintCheck`。没有新写任何复算逻辑 —— 与 `/report/[id]` 共用 `lib/data.ts` 的 `getFingerprintCheck`（内部走 `lib/fingerprint.ts` + Web Crypto，进程内 `Map` 缓存），故两页的指纹结论同源、不可能互相矛盾。；② 为什么 async 在这里是安全的：`/trace` 是静态路由（非 `generateStaticParams`），构建期 await 出结果、直接写进预渲染 HTML，运行时不发任何请求、不引入任何客户端 JS；实测 `next build` 后 `/trace` 仍是 `○ (Static) prerendered as static content`，`app/trace/page.tsx` 体积 976 B（未增加客户端负担）。；③ 一处易错点（已在渲染产物中逐一核对）：`grep 复算不一致` 在 `/trace` 的 HTML 里命中 1 次，看着像失败态 —— 实际那是「契约版本」一行的静态文案（「指纹由『字典序递归排序键 → 无空格 JSON → SHA-256』计算，任一处改动都会导致复算不一致」）。真正表达结论的是 `复算一致` 徽标（1 处）+ note「复算一致：内容与声明指纹相符」（1 处）+ 复算值 `sha256:9c9010ee…`。教训：用单个关键词数断言「有没有失败」，会被同页的普通说明文案误伤 —— 故 `check_render.mjs` 里改断言结论串而非仅断言「不一致」缺席。；④ 验收（全实测）：`tsc --noEmit` 0 错；三道构建期门禁全绿（`sync-assets` 12 份样例 / `contract-check` 6 项含资产 12/12 与指纹 16/16 / `check-fonts` A1–A4 + B0/B1 + C1 926 码位零警告）；`next build` 编译 ✓ / 类型 ✓ / 21/21 预渲染 ✓ / Export 3/3 ✓；`.work/verify_2_7.mjs` 通过（一致率 0.74 = 106/144、ECE 0.12、三桶逐值相等，未受影响）；`.work/check_render.mjs` 断言 35 → 38、38/38 全通过（新增 3 条：`/trace` 的「复算一致」徽标 / 「现场复算」说明 / 「内容与声明指纹相符」结论；调色板清零 9 路由 0 处 / 9 路由 200 / 深链 prompt 162 字符 / 4 张 SVG 图 保持通过）。 | `web/app/trace/page.tsx`、`.work/check_render.mjs`、`.work/sync_master_plan_c038.mjs`（本机脚本）、本总纲 | `faf17789-2885-49e6-ac87-2b3b8d4f03bc` | [C-034–C-038](C-034-C-038.md) |
