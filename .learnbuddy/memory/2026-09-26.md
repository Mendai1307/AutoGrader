# 2026-09-26 · 工作日志（承接 2026-09-25，会话跨零点）

> 本文件为 append-only。会话 C-028 起于 2026-09-25 23:49，跨零点完成，故日志落在 09-26。

## C-028 · 按设计使用流程审查 + 一致性修正 + 死代码清理

**诉求**（用户）：`@"步骤路径规划书_v1.1.docx" @"项目总纲与同步状态.md" @"项目目标定义书_v1.1.docx"`
—— 按设计的使用流程审查项目，debug，并进行各项引用 / README / docx 文档的一致性修正和死代码清理。

**前置发现**：`4d126b42-…`（2026-09-25 23:47）是**同一诉求的会话但被中断**（只引用了两份 docx、
无总纲），无产出 → 已作为 **C-027** 补录进总纲 §九。

### 一、基线实测（改动前）

- 六脚本自测 **61 项**全通过（T1 11 / T2 8 / T3 7 / T4 13 / T5 13 / T6 9）——
  总纲 §三 原写「54 项（T4 8 / T5 11）」**已过时**（C-025 给 T4/T5 加检查项后未回填）
- T1 解析三份 docx 全部 `status=ok`：目标定义书 v1.1 = 25 标题、规划书 v1.1 = 19、口径变更说明 = 7

### 二、已修（10 个文件，21 增 21 删）

1. **「三重校验」残留 6 处 → 六项校验**：`skills/README`（S5 行）、`S1` 规则 6、
   `SYSTEM_PROMPT §2`、`T5` 规格小节标题、`docs/contract.md` §3 与 §10.1、`contract_validator.py` docstring。
   其中 3 处属**同句自相矛盾**（标题写「六项检查」、括号写「与 S5 的三重校验对齐」）
2. **S4 上界口径错（P1 正确性）**：`skills/S4-score-calculator.md` 规则 3 / 4 仍写
   `[0, 100 − W_r]` 与「上限 100 − W_r」，而**全项目其余 13 处**都已是 `weightIncluded`。
   S4 又是「口径的唯一定义处」（skills/README §五 指定）→ 已改为
   `[0, Σ已计入 weight]` / 「上限 = 已计入权重合计」并补 ⚠️ 说明
3. **T3 出参字段名与原因枚举**（以脚本实际输出为真源对齐）：
   `resolved[].blockDigest` → `blockTextDigest`（`docs/contract.md` §3.2/§11.1/§11.2 与 `T3` 规格，共 4 处）；
   补 `quoteDigest`；删脚本**从未产出**的 `anchor-out-of-range`、补 `block-ref-not-found`；
   `evidence[].quote` 来源由 `blocks[].text` 改为 **`rawText`**（这条是实质错误：T1 自 P2-17 起分两栏，
   写 `text` 等于把「逐字引用」从源头解释错）
4. **死代码 3 处**（均 grep 验证零引用）：T4 `WEIGHT_SEQUENCE` 常量（还会误导读者以为 T4 校验权重序列）、
   T1 `Builder._code_page` 属性、T5 `check_bands()` 的 `idx` 形参（含 2 处调用点）
5. **总纲非冻结区**：§三 自测项数 54→61；§六 git 事实更正（**已是 git 仓库** master + 2 提交）
   与目录树补 `工具使用说明.md`；§九 补录 C-027 + 新增 C-028；顶部「最后更新」→ 2026-09-26

### 三、端到端实跑查出的 P0 级契约冲突（**未改，待裁决**）

按设计流程实跑 `T1→T2→T3→T4→T5`，在最后一步暴露：

- `ReviewResult.schema.json` 把 `Total.grade` 定义为 `ScoreLevel` 枚举（excellent/meeting/partial/notMet）
  或 `null`，而 T4 满权重时实际产出 `A/B/C/D/F` → **T5 报 `schema.oneOf @ $.total.grade`、退出码 1**
- 实测对照：同一份结果把 `grade` 置 `null` 则六项全过（exit 0）
- 影响：**模式 2 的正常产出会被判为不合规**；12 份历史 1.0.0 资产不受影响（无 `total` 子对象）
- 建议改法：`Total.grade` → `string | null`，并同步 `docs/contract.md` §5.4 与版本号
- **没直接改的理由**：落在未点名的**冻结契约层**，且契约要求「两处同时修订 + 版本递增」→ 属「先说明、不擅动」

### 四、交付与验收

- 报告落 `.work/一致性修正与死代码清理_2026-09-26.md`（含完整裁决清单 4.1–4.3）
- 验收：六脚本 `py_compile` 通过；`--self-test` **61 项全通过无回归**；
  残留扫描（`三重校验`/旧式上界/`blockDigest`/`anchor-out-of-range`/`blocks[].text`/
  `WEIGHT_SEQUENCE`/`_code_page`）在交付物层面**零残留**
- 副作用清理：运行产生的 `__pycache__/` 已删除；自查无遗留 python 进程

### 五、待用户裁决（未执行，详见报告第四节）

- **[P1]** `total.grade` 契约值域冲突（上文第三节）
- **[P1]** 目标定义书 §4.5「控制 36 小时风险」与同文档「忽略时间限制」冲突 → 建议改「范围失控风险」
- **[P2]** 跨文档编号撞车：目标定义书 §4.1 用 `T1–T5`，规划书 §1.3 用 `T1–T6` 指六个工具（新前缀待用户定）
- **[P2]** 规划书 §1.6 表首列 `S1–S6` 与 Skill ID 撞名
- **[P2]** S6 输出列「（批量时）班级级共性问题」悬空（批量未设计；模式 4 已排外）
- **[P2]** `工具使用说明.md` 实为对话原文粘贴（含过程性旁白与闸门快照），不像交付物
- **[P3]** 根 `err5.txt` 为 0 字节；根仍无 `README`；T4/T5 的 `--out` 无 try；T5 运行产生 `__pycache__`

---

## C-028 续（同日 00:20 起，按用户裁决执行）

**用户裁决**：① 允许修改定义书与规划书（§4.1 → A1–A5；§1.6 表首列 → M1–M6）；② 处理 S6 悬空引用；
③ 忽略 `工具使用说明.md`；④ 执行那批优化项（T4/T5 `--out`、`err5.txt`、根 README、`__pycache__`）。

### 已执行

1. **S6 悬空引用**：删掉输出第 3 条「（批量时）班级级共性问题」（批量「当前未设计」+ 形态属已排外的模式 4）。
   grep 复核设计面仅此一处
2. **T4 / T5 的 `--out` 补 try/`OSError`** → 结构化报错 `kind: "output-error"` + 退出码 2，
   与 T1/T2/T3/T6 写法对齐。**实测**：`--out` 指向不存在目录时两脚本均 exit=2、结构化、无裸 traceback
3. **删 0 字节 `err5.txt`**
4. **`contract_validator.py` 增 `sys.dont_write_bytecode = True`**（置于 import T4 之前）。
   **实测**：清空缓存后跑 T5 全流程（含 importlib 加载 `contract/fingerprint.py`）与 T4，
   两处**均不再生成 `__pycache__`**
5. **新增根 `README.md`**（开发期版本）：核心范式 / 怎么跑怎么验 / 目录导航 / 硬口径速查 /
   当前进度 / 6 条已知未闭合项 / 文档索引。**README 里记录的每条命令都实跑验证过**
   （六脚本自测 61 项、`fingerprint.py --self-test`、`fingerprint.mjs --check` 跨语言一致、T6 list 退出码 2）
6. 总纲非冻结区同步：§六 目录树补 `README.md`、git 备注更正；§九 C-028 行补记本轮

### 未执行（等裁决）

- **三份 docx 的编号 / 措辞改动**。用户给了「§4.1 用 A1–A5」，但**同文档 §4.3 已经有 A1–A5**
  （AI 工具使用维度的关键举措）——照字面改会让同一章出现两张 A1–A5 的表。
  按「连带修改属第③层 —— 不碰、把两处一并提出」回报待裁，**本轮 docx 一处未动**。

### 顺带学到（已写进长期记忆）

- 运行 T5 会生成 `__pycache__` 有**两条路径**：import 同目录 T4、以及 importlib 加载
  `contract/fingerprint.py`。`sys.dont_write_bytecode = True` 一处即可同时盖住两条
- 用 importlib 加载 `contract/fingerprint.py` 的自测脚本本身也会在 `contract/` 下建缓存——
  一次性脚本跑完要顺手清（本轮已清）
- 删目录走 `rm -rf` 会被 safe-delete 钩子按 MSYS 相对路径拒绝；须用 PowerShell
  `Remove-Item -LiteralPath "<绝对路径>" -Recurse -Force -Confirm:$false`

---

## C-028 续二（同日 00:42–00:50）：docx 编号与措辞修订

**裁决**：「§4.1 用 A1–A5」经回报**与同文档 §4.3 已有的 A1–A5 撞名**后，用户改裁 **§4.1 用 I1–I5**；
另裁「§4.5 的 36 小时风险一并改为**范围失控风险**」「**《口径变更说明》不碰**」。

**执行**：走 `tencent-docs-routing` → `tencent-local-office-edit`（按技能要求先 `edsdk.py list` 验通 +
对 `create_doc` / `doc_insert_markdown` / `save_file` 三个工具各跑 `schema`）。
`.work/md/*.md` 逐条改 → `create_doc` + `doc_insert_markdown(markdown=file://…)` + `save_file`
→ 就地覆盖两份 v1.1 docx（旧版已备份到 `.work/backup_v1.1/`）。

**改动**：目标定义书 §4.1 关键举措 `T1–T5 → I1–I5`（5 行）+ §4.5「控制 36 小时风险 → 控制范围失控风险」（1 处）；
规划书 §1.6 表首列手段序号 `S1–S6 → M1–M6`（6 行）。
**「同步修改引用」是空操作**：grep 确认这两组编号没有任何跨处引用。

**验收（本次新增的可复用方法）**：
1. `.work/verify_v11_rev.py` 回读新 docx → 17/17 判据通过（新表述 5 / 旧表述已清 3 / 对照项 6 / v1.0 原稿未动 3）
2. **逐块比对新旧 docx**（用 backup 做对照）：两份**各恰好 6 个块不同**，
   块数 `205→205` / `125→125`、标题数 25 / 19 不变 → **证明整文重生成没有带来连带变化**
3. v1.0 两份原稿的标题数与旧编号均未变 → 原样存档成立

**未做且留了新缺口**：《口径变更说明》§五 写着「规划书 `1.6` ｜ 复核后仍一致，**未改**」，
而 §1.6 刚被改 → 对当前 v1.1 已不成立。已写进报告与总纲待确认，**说改就补一行**。

**资源释放**：两个 editor_sdk 实例已 `close_file`，`get_pool_status` 显示 `pool_size: 0`。

---

## C-028 续三（同日 00:54–01:10）：修 P0 —— 契约 1.1.0 → **1.2.0**

**诉求**：修复 `total.grade` 值域冲突（改法 = `grade → string | null` + 同步 `docs/contract.md` §5.4 与**版本号**）。

**版本号判定**：放宽既有字段取值域（接受集合变大、旧数据仍合法）→ **MINOR（1.1.0 → 1.2.0）**。
这条档位原本**不在** `docs/contract.md` §二 的规则里（只有「新增 optional→MINOR / 新增 required 或改语义→MAJOR」），
本次**补上了「放宽既有字段的取值域 → MINOR」**，让这次定性有据可查。
`Rubric.schema.json` 内容未变，因与 `ReviewResult` 同属一份契约（历来同版本号）而同步 `$id`。

**改动面（9 类）**：2 份 schema（`$id` + 新增 `$defs.Grade`，带「与 ScoreLevel 同名不同物、取值域开放」的说明）；
`docs/contract.md`（标题、文件头、§二 增规则、**§5.4 新增取值域表**、§十二）；
版本号引用 9 处（`SYSTEM_PROMPT` ×4、`tools/README`、`T5` 规格样例 ×2、`T6` 规格、`T4` 脚本 docstring、
`contract_validator.py` 自测夹具 ×6、`README.md`、总纲 §三/§五）。

**有意未改**：`contract/fingerprint.py` / `.mjs` 的 `version: 1.1.0` —— 那是**模块自己的版本**（算法一字未动），
改成 1.2.0 会反过来暗示算法变了。

**验收（三项 + 一组负向对照）**：
- 六脚本自测 **61 项全通过**
- **当初失败的用例**（`grade='A'`）→ T5 **exit=0 / valid=True / errors=[] / skipped=[]**，六项全 pass
- **12 份历史资产回归**（`../AutoGrader/frontend/public/results/`，用随包契约、未传 `--schema`）：
  **T5 六项全过 12/12 且 skipped 为空；指纹复算与声明值相符 12/12**
- 负向对照：`'A'`/`null` 放行；`123`/`true`/`{}` 一律 exit=1 报 `schema.oneOf` → **放宽的边界精确落在「等级名或 null」**，
  没有放宽成「不校验」

**脚本**：`.work/verify_contract_1_2_0.py`（可复用：A 自测 / B 定向用例 / C 历史资产回归）。

**仍开放**：R1（读通道）；12 份历史资产的 `total` 子对象未回填（`docs/contract.md` §12 的「可自动推导但不做」口径，
属非点名范围）；总纲**第四节（冻结区）**写「耦合枢纽：**Zod 定义**的 `ReviewResult` 契约」而现行真源是 JSON Schema
——按「冻结区须逐次征询」只提不改，已登记。

---

## C-028 续四（同日 01:17–01:35）：R1 闭合 + 回填落点取证

### 一、R1 已闭合（用户裁决：走平台内置读取能力）

不扩展 T6 的 `result` 类别，改走平台内置**文件读取**能力。通道定为：
**教师给绝对路径（或直接粘贴 JSON）→ 内置文件读取逐字读入 → 必要时用 T5 校合规 → 只读回放**；
读入内容**不得凭记忆复述 / 改写 / 回写**。

改动 5 个文件：`S7-review-checklist.md`（5 处：依赖 Tool 行 / 前置行 / 小节标题 / R1 行 / 末段结论）、
`docs/contract.md` §十三（标题「尚未闭合」→「均已闭合」+ R1 行）、`SYSTEM_PROMPT.md` §9（新增一条读入纪律）、
`README.md` §六（移入「已闭合项」并顺带移出已修的 `total.grade`）、总纲 §五 模式 3 行（→ 无外部阻塞）。

### 二、回填 `total`：**实测出岔路，未动手**

**关键事实（本轮新查）**：v0.1 的 Zod 契约**顶层是 `.strict()`**（`schema.ts:13`：
「全部对象使用 `.strict()`：未知字段一律拒绝」）且**顶层没有 `total`**。
双向实测（`.work/zod_check.mjs`，注意 schema 参数必须传 `file://` URL，否则报 `ERR_UNSUPPORTED_ESM_URL_SCHEME`）：

| 文件集 | v0.1 Zod |
|---|---|
| 原始 12 份 | **12 pass / 0 fail** |
| 同 12 份加 `total` 后 | **0 pass / 12 fail** |

**两条连带后果**：① `docs/contract.md` §12 规则 1「12 份一行不改、继续通过 Zod 与 T5 双重校验（12/12）」
将不再成立；② **v0.1 站点会降级**——`frontend/lib/data.ts:206` 读资产时会调 `validateReviewResult`，
加 `total` 后这 12 份判无效，`/eval` 与 `/report/[id]` 页进入「未找到 / 校验失败」态。
（v0.1 未推送、工作树干净、分支 `main` → 一次 `git checkout` 可完全回退。）

**已查到的唯一文档指定落点**：总纲 §六「⬜ 规划中」里 **`web/public/results/` = 「评阅结果 JSON 资产」**
（交付物二的一部分，尚未创建）。

**已就绪**：回填样本已生成在 `.work/v01_backfill_probe/`（`total` 纯推导自 `scores[].weight`，
符合 `docs/contract.md` §12 规则 4 的「可自动做」口径）。落点一确定即可一步落位。

### 三、落点判定与执行（已落地）

`总纲 §六 规划` 里 **`web/public/results/` 已被指定为「评阅结果 JSON 资产」**——全项目唯一被文档指定给
「评阅结果」的位置。选它同时满足：回填做了 / §12 规则 1「v0.1 原件一行不改」仍成立 / v0.1 已发布站点不降级 /
不新建未规划目录。

**执行**：读 v0.1 原件 → 加 `total` → **重算 `resultFingerprint`** → 写同名副本到 `web/public/results/`。
`schemaVersion` 保持 `1.0.0`。

**改动面**：每份**只多 7 行 `total` 块 + 改写 1 行指纹**，不动任何已有字段。
⚠️ **首轮两个坑**：① **不回填指纹就必挂**（指纹覆盖整个对象含新键）→ T5 报 `fingerprint.mismatch`，12/12 全挂；
② 我首轮用 Python 文本模式写文件，在 Windows 上把 LF 写成了 **CRLF** → diff 呈「全文件替换」，
必须显式 `newline="\n"`。两处在 `docs/contract.md` §12 新增的**规则 5** 里都写清了。

**验收**：新落点 T5 六项全过 **12/12** 且 `skipped` 为空；指纹自洽 **12/12**（12 份指纹值全部变化，属必然）；
v0.1 原件 **Zod 12/12 未受影响**、`git status` 为空 → **v0.1 一字未动**。

---

## C-029（同会话 · 同日 01:41–）：解冻 Workflow / Evaluation 两层，执行《规划书》1.4、1.5

**用户裁决**：解冻 workflow、evaluations 层，执行实现步骤 **1.4**（Workflow 层）与 **1.5**（Evaluation 层）。

### 一、前置事实：用户把文档移进了 `documents/`

2026-09-26 01:38，用户把工作区根的**总纲、契约冻结方案、AUDIT、工具使用说明、5 份 docx** 全部移入
**`documents/`**（内容未改，仅位移）。`documents/` 下有两个 `~$…docx` **Word 锁定文件** → 两份 v1.1 docx
**正被 Word 打开**，本轮**未读写**它们。总纲 §六 与 README 已同步该位移。

### 二、Workflow 层（1.4）→ `agents/workflow/`（4 文件）

- `README.md`：五环一览 + **与三种模式的关系**（只有模式 2 走五环；模式 1 是「T6→S1→S5→保存」两步 + 自检；
  模式 3 只读无编排）+ 完成判据对照
- `W1-five-agent-pipeline.md`（**1.4.1**）：五环映射（Agent ↔ Skill ↔ Tool ↔ 契约落点）+
  逐环**四栏**（输入/输出/置信度/证据引用）+ 各环硬闸门 + **环间交接物表**（全是文件/结构化片段）
- `W2-orchestration-modes.md`（**1.4.2**）：单会话分步 · 多会话分环 + **共同硬约束 6 条** + 选型判据
  （**不含任何时间估算**，项目既定口径）+ **等价性 4 条判据**
- `W3-failure-and-retry.md`（**1.4.3**）：F1–F3 三原则 + **8 个状态标记闭环词汇表** + **12 类失败处置** +
  **回退边界表**（能退到哪 / 绝不能退到哪）+ 六脚本退出码与 CI 判定映射

**未闭合**：W2 的「两种形态实跑同一份报告」需**真实 LearnBuddy 会话**（AI 在对话侧跑），
本轮只保证规格齐备 + 产物形状可校验——已在 W2 §四 与 README §六 显式标注。

### 三、Evaluation 层（1.5）→ `agents/evaluations/`（6 文件）

`README.md` + **`metrics.md`（口径真源，只在此定义一次）** + `scripts/evaluate.py`（自测 **17 项**）+
`scripts/regression_gate.py`（自测 **10 项**）+ `baselines/2026-09-26.json` 与 `.report.md`。

**四项指标**：总分 MAE / 逐项命中率（12 行）/ 档位一致率（同口径汇总值）/ 置信度校准（分桶阈值**取契约 §八**
的 0.60、0.80，汇总 ECE）。另加 **MAE 的 `byTier` 分组视图**，用途是复现并主动披露「差档 MAE 偏高」。

**两处刻意取舍（已写进 metrics.md）**：① ECE 用**已展示的 2 位小数**加权 → 换「读者可手工复算」；
② 档序取**金标准中首次出现的次序**（数据驱动，不写死枚举）。

### 四、实测：三个公开数字被独立复现

| 指标 | 本轮 | v0.1 公布 |
|---|---|---|
| 总分 MAE | **3.05** | 3.05 ✅ |
| 差档 MAE | **8.0**（3 份） | 8.00 ✅ |
| 单份最大绝对偏差 | **14.0（sample-04）** | 14.0 ✅ |
| 档位一致率 | 0.74（106/144） | 新增 |
| ECE | 0.12 | 新增 |

**门禁三态**：基线对自己 → 0；三项人为劣化 → 1（均标 REGRESSED）；`--tolerance 0.5` → 0。

**一处事实校正**：v0.1 README 写「差档 **4 份**」，金标准里 `tier=差` 实为 **3 份**；份数以数据为准，MAE 不受影响。

### 五、两处自身失误（都被自测/输出抓出来）

① 自测期望值写错（把 conf=0.5 误算进 medium，实际按契约阈值归 low）——**代码是对的**；
② `itemName` 在内部转换中被丢掉（输出全为 `null`）→ 已补透传 + 加 `itemNameCarried` 断言。
另修同族卫生问题：`evaluate.py` 的 importlib 加载会在交付目录留 `__pycache__` → 加 `sys.dont_write_bytecode`。

---

## C-030（同会话 · 同日 02:21–）：建随包资产根 + 解冻打包层，做成可召唤的本地专家

**用户裁决**：① 建立随包资产根 `agents/tools/assets/`；② 解冻打包层，**参照附件**
`C:\Users\Mendai\.learnbuddy\plugins\marketplaces\my-experts\plugins\frontend-design-expert.zip`
做映射并补齐文件，把专家包做成**可以在 LearnBuddy 召唤的本地专家**。

### 一、任务 1：随包资产根（`agents/tools/assets/`）

四类落定 —— `template/` 1 份通用 Rubric 样板、`rubric/` 空（待教师写入）、`sample/` 12 份样例报告、
`gold/` 1 份金标准 manifest；内容全部来自 v0.1 `demo/`，**SHA-256 逐字节核对一致**；另附 `assets/README.md`。
**实测** T6：template/sample/gold = 1/12/1 项 exit 0；`rubric` 0 项 **exit 1（未命中，正确语义）**。

### 二、任务 2：打包层

**参照包提供的权威格式**（`frontend-design-expert/`）：`.codebuddy-plugin/plugin.json` + `agents/<agentName>.md`
+ `avatars/` + `skills/<名>/SKILL.md(+references/)` + `README.md`；另有市场注册表
`my-experts/.codebuddy-plugin/marketplace.json`。

**关键映射裁决**（平台的插件根只认 `agents`/`skills`/`bin`/`avatars`，**没有 workflow/evaluation/tools 槽位**）：
- 8 个技能 → 8 个 `skills/<名>/SKILL.md`
- **Workflow / Evaluation 各映射为一个「规格技能」**（`SKILL.md` + `references/`）
- **Tools 与两个评测 CLI 合并进 `skills/autograder-tools/`**：`scripts/` + `rules/` + `contract/` + `assets/` 同级
  → 这样 **T2 的 `../rules`、T6 的 `../assets`、T4/T5 向上查找 `contract/` 三处路径解析一行代码都不用改**（已核实）
- Prompt → `agents/autograder-expert.md`（按该文件自己的说明**剥离头部状态块与文末附录**）

**产物**：`build_expert.py`（自测 **21 项**）+ `packaging/expert.manifest.json`（映射唯一真源）+
`packaging/PLUGIN_README.md` + `packaging/dist/autograder-expert.zip`。

**官方链路实跑**：`init_expert.py` 建骨架 → 装配 **54 文件** → `validate_expert.py` **✅ valid** →
`register_expert.py` **已注册**（原 `frontend-design-expert` **被保留**）→ `package_expert.py` → 52 文件 / 197.9 KB。

**包内验收（本轮最强证据）**：从**包内位置**跑 8 个 CLI 自测 **88 项全通过**；T6 从包内找到样板；
T5 从包内**自解析**到 `skills/autograder-tools/contract/ReviewResult.schema.json` 且 `valid=True`。

### 三、本轮踩到 / 学到的（已写进长期记忆）

- 官方校验器**强制**专家目录必须在 `<CONFIG_DIR>/plugins/marketplaces/my-experts/plugins/` 之下；
  本机 `WORKBUDDY_CONFIG_DIR` 已设为 `C:\Users\Mendai\.learnbuddy`（不是默认的 `~/.workbuddy`）
- 官方 `parse_md_frontmatter` 只做**朴素正则 + `strip('"')`**，**不会反转义** → 描述里若含 ASCII 引号，
  校验器读到的是带反斜杠的原文。故清单里的描述统一**不用 ASCII 引号**（用「」）
- `plugin.json` 的 `tags` 与 `quickPrompts` **必须各 3 个**（否则 ERROR）；`defaultInitPrompt` 应与 `quickPrompts[0]` 一致（WARN）；
  `displayDescription.zh` 建议 40–50 字（WARN）
- `package_expert.py` 排除：`__pycache__` / `node_modules` / 根 `dist` / 隐藏路径（`bin`、`.codebuddy-plugin` 除外——
  实际允许的隐藏路径是 `.codebuddy-plugin`）；`.gitkeep` 不入包
- `register_expert.py` 会**写 `.created-by-session`**；我首次查看时用了**混合分隔符路径**
  （`C:\...\.learnbuddy/plugins/...`）导致 `cat` 报 no such file，重跑注册后确认文件确实存在 —— **不是脚本缺陷**
- 装配脚本必须显式 `newline="\n"` 写文件，否则 Windows 文本模式会把 LF 写成 CRLF

### 四、已知未做

- **头像**：`avatars/` 为空（仅 `.gitkeep`）。配置指向 `avatars/expert.png`，缺文件时官方校验器**只告警不报错**，
  故包仍有效。生成头像需图像生成工具（**会产生额外额度消耗**），本回合未执行 —— 待用户决定是否生成
- 两种编排形态的实跑对照（需真实会话）

---

## C-031（同会话 · 同日 02:56–）：生成头像的尝试 + 打包器缺陷修复

**用户指令**：生成头像。

### 一、结论：本环境**不可用**图像生成工具

按官方 `references/avatar-spec.md` 提取特征、按规范构建了 prompt（背景色调取 `10-ProjectQuality` 规定的
**green-emerald**），执行时：

- `ToolSearch` 以 `ImageGen` **精确名**查找 → 无匹配
- 以关键词（生成图片 / 文生图 / text to image）检索 → 只找到 `VideoGen` 与 Ardot 系列，**无图像生成**
- 按规程先跑 `find-skills`（它是技能检索入口，本身不提供模型能力）
- 最后用 `DeferExecuteTool` 直呼 `ImageGen` → 明确错误：
  **`Tool "ImageGen" is not available in the current environment or configuration`**（非参数问题）

→ 按头像规范的「生成失败处理」条款：把**推荐 prompt + 两种补法**写进 `packaging/PLUGIN_README.md` §六，
并重新装配使包内 README 同步带上该节。**prompt 的提取依据逐项列表**（角色身份 / 外观特征 / 表情气质 /
背景元素 / 背景色调 / 质量后缀），避免日后被误认为套模板。

### 二、修掉打包器的一个**破坏性缺陷**（本轮最重要的一件事）

**现象**：原先的 `clean_stale()` 是「先整体删目录、再写入」。重复装配时：① 被宿主「单轮删除 >50 项需确认」
的安全闸**打断在半途**，包被破坏（README 被删）；② 我改成「增量删除残留」后，自测的 `pruneKeepsDeclared`
**抓到我自己的实现把 24 个目录删掉了**（含 `agents/`、`.codebuddy-plugin/`）——自测救了这一把，
但运行时已造成一次破坏。

**修法**：清理改为**默认只报告、不删除**（`stale_items()` 只列残留；要删须显式 `--prune`）。
理由写进函数 docstring：**装配器不该具备破坏性**——清理残留是人的判断，不是工具的自作主张。
自测改为 5 条：残留被报出 / 清单内文件不被报 / **任何情况下都不删任何文件** / `avatars/` 与会话标记不算残留。

**恢复与验收**：重新装配 → 54 文件、**无残留**、官方校验 **valid**、已注册；
包内 8 个 CLI 自测 **88 项全过**；T5 从包内自解析契约 `valid=True`；
Python 复核 **55 文件**（含 `.created-by-session`）、**清单与落盘零差异**。

### 三、两条读数事实（新，别再踩）

- **PowerShell 工具在本沙箱不回显 stdout**（多次 exit 0/1 却无输出）→ 不要用它做校验；
  可靠做法是 Python 脚本化的 `os.walk` + `os.path.exists` / `json.load`
- **⚠️ `.created-by-session` 会被外部清掉**：`register_expert.py` 刚写完时确实在
  （同一进程链内 Python 复核 = 55 文件、标记存在、内容正确），但**稍后的独立进程再看只剩 54 文件、标记消失**。
  **参考包 `frontend-design-expert/` 同样没有这个文件** → 最可能是宿主对市场目录的同步/规范化行为，
  **不是缺陷**。判断包完整性的口径 = **54 个交付文件**（官方 zip 为 52，差额是 2 个 `.gitkeep`）。
  ⚠️ **我一度把此现象解释为「bash 对混合分隔符路径读数不可靠」——那是错的**：
  bash 当时读到的「54 + 缺失」才是事实，矛盾来自**时间差**而非路径。已改正记忆与报告

---

## C-032（同会话 · 同日 03:14–）：按规划书核对 1.6 之前的步骤

**用户指令**：核对规划书，1.6 之前的步骤是否实现且自检通过。

### 一、核对方法

判据**逐条取自 docx 原文**（T1 解析 `documents/…规划书_v1.1.docx`，status=ok / 19 标题 / 125 块），
范围 = 路径 0 的 `0.1–0.6` + 路径一的 `1.0–1.5`，共 **32 条**。
核对台落在 `.work/audit_plan_pre_1_6.py`（一条命令重跑全部结论）。

结论分四档：`PASS`（可脚本复算且实测通过）/ `SPEC-ONLY`（规格齐备，但判据要求**平台侧真实会话**
才能验）/ `DEFERRED`（判据原文自明前置）/ `FAIL`。

### 二、结论：32 条全部有结论，**未通过 0 条**

- **PASS 17**：0.1–0.6（6）+ 1.0（1）+ 1.3.1/3/4/5/6（5）+ 1.4.1/4.3（2）+ 1.5.1/2/3（3）
- **SPEC-ONLY 14**：1.1.1–1.1.5（5）+ 1.2.1–1.2.8（8）+ 1.4.2（1）
- **DEFERRED 1**：1.3.2 的**跨端一致性**（判据原文写明「待交付物二建立后校验」）

亮点证据：0.1 双份 12/12 通过六项校验；0.3 指纹 Python/JS 双侧 12/12；0.5 **31 条索引全部可定位**
（C-012 按记载在另一工作区目录，已核实文件存在）；1.3.4 含待复核项时 `upperBound == weightIncluded == 60`
（旧式会给 100）；1.3.6「目录不存在 exit 2」与「库为空 exit 1」可区分。

### 三、两处**我自己的误判** + 一处真缺口（如实记录）

1. **1.3.1**：我最初要求 md/docx/txt **全部** `status=ok` —— 实际纯文本无标题返回 `partial` **是正确行为**
   （「可解析」≠「识别出标题」）。已改为「格式被正确接住并给出结构化输出」为准。
2. **1.3.5**：我最初用 `definitions` 探测「不支持的关键字」—— 而 **`definitions` 本就在 T5 白名单里**
   （属注解关键字，与 `$defs` 同级），T5 不报错是对的。已改用 T5 自测所用的 `uniqueItems` → exit=2 ✓。
3. **真缺口**：判据要求「**四类**输入均可解析」，而**本机没有任何 PDF 样本**（本工作区、v0.1、同级目录都搜过）
   → PDF 分支**未验**。既不算 FAIL（其余三类已过，缺的是样本不是实现），也不算 PASS（结论表已显式标注）。
   后续拿一份真实文字层 PDF 补测即可。

### 四、14 条 SPEC-ONLY 的口径（重要，别被读成已完成）

这 14 条判据描述的是**智能体在对话里的行为**（「对超范围请求能明确拒绝」「输出可被 schema 直接解析」
「无证据不得高置信」「两种形态都能跑通同一份报告」），只能在**真实 LearnBuddy 会话**里验。
本工作区能做、也已做完的是：**把可机器校验的部分全部实测通过**（契约 / 口径 / 命名 / 工具行为）。
报告里已明确写「这 14 条不应被读成『已完成』，而是『待平台侧实跑验收』」。

---

## C-033（同会话 · 同日 03:25–）：1.6 与 1.7 一并审查

**用户指令**：1.6 和 1.7 也一并审查。

### 一、结论：16 条全部有结论，**未通过 0 条**（PASS 13 / SPEC-ONLY 2 / DEFERRED 1）

**1.6（M1–M6，硬指标「每次都产出合规 JSON」）**
- **M1 PASS**：塞一个未定义字段 → T5 报 `schema.additionalProperties`（结构漂移被挡住）
- **M2 PASS**：档位改成枚举外的 `good` → T5 报 `band.level-not-in-rubric`（少自由度被契约钉死）
- **M3 PASS**：W1 五环 + Prompt 里的**中文分步链路**（`S2 报告解析 → … → S6 评语生成`）
- **M4 PASS**：校验失败时 `errors[]` **每项带 `fixHint`**（可驱动重试）+ W3/S5 写明「限定轮次 → 待人工」
- **M5 PASS**：**14 组语义层畸形输入**（空文件 / 超长 2.7MB / 二进制垃圾 / **无文字层 docx** / 路径不存在 /
  缺字段 JSON / 越权写入）全部结构化返回、**裸 traceback 0 例**；无文字层 docx → `status=failed` 且 `failures` 非空。
  另测**语法层**（flag 拼错）：六脚本一致 exit=2 且无 traceback（6/6）
- **M6 DEFERRED（唯一实质缺口）**：样例集回归**已具备**（12 份样例 + evaluate + 门禁 + 基线），
  但 **「对抗样本集」尚未单列** —— `assets/` 四类里没有对抗样本目录；且素材需**新造**（空报告/全截图/缺章节/超长…），
  不能靠复制 v0.1 的 12 份样例补齐

**1.7（6 条硬约束 + 明写的四步完成判据）** —— 硬约束 1–6 **全部 PASS**：
脚本落 **`skills/…/scripts/` 首选路径**（未用 `bin/`）、包内 **11/11** 技能目录含 `SKILL.md`、
frontmatter **无 `tools:`**、落点正确且已注册、`python3` 优先/`python` 回退已写明。
判据①按 README 装配 **PASS**、②运行注册脚本 **PASS**、③「专家中心人眼可见」**SPEC-ONLY**、
④「给它一份新报告能产出合规 JSON」**SPEC-ONLY**（需真实会话）。

### 二、又两处**我自己的断言写错**（已更正）

1. **M3**：拿英文 Agent 名（`Parser`/`Feedback`）断言 Prompt —— 而 Prompt 写的是**中文分步**。
2. **1.7-6**：按字面找「python3 优先」，实际措辞带反引号（「`python3` 优先，`python` 回退」）。
3. 另有一处**分类问题**：把「flag 拼错」这种**语法层**错误混进「语义层畸形输入」探针 ——
   argparse 的既定行为是打 usage 到 stderr + exit 2，**本就不产出结构化 JSON**。已分两类分别断言。

### 三、补记：总纲 §九 漏了两行索引

C-031 / C-032 我在日志里编了号，却**没登记进总纲 §九**（发现时只到 C-030）。已补上 C-031 / C-032 / C-033 三行，
并同步「对话 ID 说明」为「C-028…C-033 属同一会话」。**教训：日志编号与索引登记必须同轮做完，否则会漏。**

### 四、全貌

| 范围 | 条数 | PASS | SPEC-ONLY | DEFERRED | FAIL |
|---|---|---|---|---|---|
| 1.6 之前（0.1–0.6 + 1.0–1.5） | 32 | 17 | 14 | 1 | **0** |
| 1.6（M1–M6）+ 1.7 | 16 | 13 | 2 | 1 | **0** |
| **合计** | **48** | **30** | **16** | **2** | **0** |

缺口 2 处：**对抗样本集**（资产待新造）、**PDF 样本**（1.6 前核对里记的，本机无 PDF 可测）。


---

## 交付物二 · Web Demo 开工（2.1–2.8）· 第①段：骨架 + 契约 + 数据层 + 门禁

用户裁决 13 项方向（计划存 `~/.learnbuddy/plans/stellar-thunder-turing.md`）：复用 v0.1 骨架 + **重做视觉**、
2.5 含 **docx 浏览器端解析**、沿用 **GitHub Actions → Pages**、**Zod 移植 + 构建期一致性检查**、
**7 个路由**、**零依赖 SVG 自绘图表**、视觉**模仿 `LBEILC/RhineLabUI`**、**引入 MiSans 分包**、
**单强调色 + 灰阶（数据语义不用红绿）**、**仅微过渡 + 白场入场 + 460ms 编号滚动**、**分 3 段交付**、
仓库根 = 整个工作区（排除 `.work/` `.learnbuddy/`）、workflow + `.gitignore` 都写好。

### 一、视觉体系（取自 RhineLabUI 的 DESIGN.md）

暖灰白底 `#F3F0EB` · 近黑正文 `#080A08` · 卡面 `#FAF8F4` · 暖灰辅助 `#77756D` ·
常规细线 `#C9C4B9` · **强调分隔线 `#AAA59A`（新增 `--rule`）** · 暖棕 `#9B7247`（唯一强调色）·
暖杏金 `#C5A16B`（仅"选中/命中"）· 低饱和赭石 `#8C4A3A`（**只留给真错误**）· radius `0.375rem`。

**修正了计划里的一条错判**：v0.1 **并非全走 CSS 变量** —— 另有约 **94 处硬编码调色板**
（amber/emerald/rose/sky/slate；集中在 `lib/constants.ts` 21 + `inspection-panel` 13 +
`score-item-card` 9 + `ui/badge` 4 + `markdown-view` 4 + `empty-state` 2）。
"只改 `:root` 就整体换肤"**对 token 色成立、对调色板不成立**。已全部改写为
**token 类 + 灰阶 + 刻度符号**：档位 `●●●●`/`●●●○`/`●●○○`/`●○○○`，置信度 3 格刻度；
`needsReview` 用 `※`、`pending` 用 `□`（契约要求二者正交且视觉可区分）。
代码块改"纸面反相"（`bg-foreground` + `text-background`）。

### 二、MiSans 字体：113 MB → 5.3 MB

- 上游 `misans-webfont@4.3.1` tarball **113 MB**（10 字重 × 约 188 分片）；四档全量 **24.4 MB**。
- `web/scripts/sync_fonts.py`（开发期工具，**构建不需要**）：字符集口径 =
  **GB2312 ∪ 站点内容 ∪ 常用标点区(U+2000–206F / U+3000–303F)**
  → **56/56/56/57 分片，225 个 woff2，5.31 MB**，聚合摘要 `sha256:b9515ee2…`。
- **上游包两处缺陷已修**：① `font-weight` 全写成 400（仅 bold 是 700）且四档家族名各异
  （`MiSans Light`/`MiSans`/`MiSans Demibold`）→ 统一 `font-family:"MiSans"` + 显式 300/400/600/700；
  ② `src` 带 `local()` 会让本机字体覆盖固定版本 → 剥离。
- 字体 CSS 放 `public/fonts/misans.css`，**url() 全为相对路径** → 与部署子路径无关，
  只需 `layout.tsx` 给 `<link href>` 拼 basePath。
- `scripts/check-fonts.mjs` 构建期门禁：分片齐全 / 聚合摘要 / 四档齐备 /
  **数据资产字符 100% 覆盖**（硬失败）+ 源码文案覆盖（警告）。
  **它抓到一个真问题**：`inspection-panel.tsx` 渲染了 `↔`，而 MiSans 根本没有该字形 → 改为「与」。
- ⚠️ 教训：**只用于匹配**的汉字不要写字面量。`lib/docx.ts` 正则里的"標題"只用于匹配样式名、
  永不渲染，写字面量会让字体门禁误报 → 改用 `\uXXXX` 转义。

### 三、跨端一致性：两阶段对拍 12/12 全绿（**闭合规划书 1.3.2 的 DEFERRED 判据**）

`web/scripts/audit-cross-end.mjs`：同一份输入分别喂 Web 端与工具链，逐条比对。

- **阶段 A 解析**（`lib/parse.ts` ↔ T1）：blocks 全量（blockId/kind/anchor/text/**rawText**）、
  结构树、summary、status、`sourceFile.digest`、空标题段计数 —— **12/12 逐字一致**。
- **阶段 B 规则**（`lib/rules-engine.ts` ↔ T2）：13 条 facts 逐条（含 `fact` 文案、阈值、锚点）、
  notCovered + 原因、stats、summaryMismatches、**rulesetDigest**、exitCode —— **12/12 一致**。
- 移植时对齐的隐蔽点：`render()` 模板语义（先条件式再简单式）、结构规则**最长模式优先排他分配**、
  统计按**码位数**计、相似度 `round(…,4)` 必须 **half-even**（不能用 JS 的 half-up）、
  rulesetDigest 要复现 **Python 风格 JSON**（键递归排序 + 分隔符带空格 + 不转义非 ASCII）。
- **两处踩过的假差异**：① 对象**键序**（Python 落盘 `sort_keys`）→ 比对改为键序无关；
  ② `sourceFile.digest` 带 **`sha256:` 前缀**（T1 的 `sha256_text` 返回带前缀），漏掉必然对不上。
- 为让脚本用原生 Node ESM 直接 import lib 模块：**开了 `allowImportingTsExtensions`**；
  脚本可见模块内的相对导入**必须带 `.ts` 扩展名**（原生 ESM 不做扩展名推断，Next 两种都行）。

### 四、契约门禁 `scripts/contract-check.mjs`

- **A 字段集合双向比对**：Zod `.shape` 反射 ↔ JSON Schema（递归展开 `$defs`/`$ref`），
  **23 个对象节点逐层一致**；另单列 7 个只作叶子类型的 $defs（避免"以为比过了"）。
- **C 真实数据回归**：12/12 资产 + 1 份夹具（`fixtures/result-grade-a.json` 覆盖 `total.grade`
  等级映射分支 —— 12 份历史资产的 grade 全是 null，覆盖不到那一支）。
- **D 指纹双实现等价**：`lib/fingerprint.ts` ↔ `contract/fingerprint.mjs` **15/15 组输入逐位相同**；
  13/13 份资产的**声明指纹 == 复算值**。
- `lib/schema.ts` 补齐 1.2.0：顶层加 `taskType`/`rubricRef`/`total`/`reviewMeta`/`doubts`/
  `reviewChecklist`/`selfCheck`；ScoreItem 加 `suggestedScore`/`overriddenByTeacher`/`pending`，
  `score` 放宽为 `number|null`；Evidence 加 `citationValid`/`blockRef`；
  新增 Total/ReviewMeta/Doubt/ChecklistEntry/SelfCheck；superRefine 补 **I5/I6/I7/I8/I9/I10/I12/I13**；
  `schemaVersion` 改形状校验（同时兼容 1.0.0/1.1.0/1.2.0）；
  `computeWeightedTotal` 改为**跳过 pending 与 null**，新增 `recomputeTotals()`（供 I13 与页面现场复算）。

### 五、本机环境三个坑（都已有绕法）

1. **`npm install` 会被 safe-delete 钩子打断**（npm 回滚清理要批量删临时目录 → 被拦 → 安装中止、lockfile 未写）。
   → 依赖改用 **v0.1 的 `node_modules` 覆盖复制**（版本与本项目**完全相同**，逐包核对过）；
   lockfile 以 **v0.1 的 `package-lock.json`**（429 条、含 8 个 `@next/swc-*` 平台包）为基准，
   再对齐本项目 `name/version/engines/依赖声明`。
   ⚠️ 期间一次复制我误判为"停滞"，其实 `next` 已复制完（6929 = 6929）；
   **判断复制进度要看文件数对比，不能只看 `find -newermt`**。
2. **目录改名/删除**：跨目录与同父目录 rename 都可能 `WinError 5`（句柄占用）；
   `rmtree`/`Remove-Item` 被钩子拦（`SAFE_DELETE_BULK_CONFIRM_REQUIRED` /
   `trash-failed: 这个系统不支持该功能`）。→ **能用的只有"移动"**：`os.rename` 到 `.work/` 成功。
3. **`next build` 会删 `.next` 里的陈旧文件 → 被拦 → 构建失败**。→ 每次构建前把旧 `.next`
   **移**进 `.work/next-prev-N`（不删），让 Next 建全新 `.next`。
   `NEXT_PUBLIC_BASE_PATH` 默认空 → 本地所见即所得（修掉 v0.1"构建用子路径、服务用根路径"的老问题）。

### 六、其它产出

- 仓库根 `.gitignore` 与 `.github/workflows/deploy.yml`：Node 22 → 跨端对拍 → `npm run build`（三道门禁）
  → `preview:check` 冒烟 → `deploy-pages`；basePath 按仓库名自动判定（用户站点为空、项目站点 `/仓库名`）。
- `web/README.md`（本地零配置跑 + 部署步骤 + 门禁说明 + 目录导览）、
  `public/fonts/PROVENANCE.md`（字体来源、裁剪口径、复现命令、许可）。
- 新页面：`app/agents`（2.3 五件套，数字**机械抽取**自仓库，不手写）、`app/upload`（2.5）。
- 新组件：`boot-curtain`（白场入场：不阻塞、可跳过、会话内只播一次、尊重 reduced-motion）、
  `rolling-number`（460ms 数字滚动，rAF 插值）、`five-piece-diagram`（长短竖线刻度轨道图）、
  `upload-inspector`（客户端核查器；语料 `await import()` 按需加载 —— 语料模块 83 KB，不点上传就不下载）。
- **偏差申报**：计划写"大文件放 Web Worker"，**未实现**。原因：docx 解析依赖 `DOMParser`，
  而 **Worker 作用域没有 DOMParser**，无法把最重的路径搬到后台线程；改为"超过 8 MB 直接拒绝并说明"。

### 七、第①段续（同日 10:45–11:05）：复核、一次自我回归、以及一个**阻断性环境结论**

**1. 我自己的回归（已修，务必记住）**
为移植 v0.1 页面，我在 10:46 用 `cp` 把 v0.1 的 5 个页面**覆盖**进 `web/app/` —— 而
**此前的轮次已经把这些页面适配过了**。表现：`tsc` 报 5 处错（`variant="success"` 已改名 `accent`；
`getContractExample` 已改名 `getTraceExample`；一处隐式 any）。已逐处改回，现 `tsc --noEmit` **0 错**。
⚠️ **教训：在有多轮中断的会话里，动手前先按 mtime 盘一遍目标目录**（`ls -la --time-style`），
确认哪些文件是"此前轮次已写过"的，再决定是新建还是改。不要凭"我刚创建过"的印象直接覆盖。

**2. `scripts/contract-check.mjs` 重写并抓出一处真缺陷**
- **跨文件 `$ref` 丢上下文**：`rubric` 是 `{"$ref":"Rubric.schema.json"}`，而 Rubric 文档内部又有
  `{"$ref":"#/$defs/RubricItem"}`。原实现把 `#/...` 一律拿 ReviewResult 当根解析 → 误报
  "`ReviewResult.rubric.items[]`：web 侧是对象，契约侧不是对象"。修法：**把 docRoot 一路带下去**。
- 现在覆盖两侧文档的 `$defs`（ReviewResult 23 个定义 / Rubric 3 个），并单列 7 个叶子类型。
- 终值：**23 个对象层 / 132 个字段零差异**；12/12 资产 + 1/1 夹具；**指纹双实现 16/16 逐字相同**；
  **12/12 份资产的声明指纹 == 复算值**。

**3. 跨端对拍复跑（`scripts/audit-cross-end.mjs`）——12/12 全绿**
阶段 A 解析（`lib/parse.ts` ↔ T1）：12 份样例的 blocks 全量一致（78/58/30/24/75/59/30/11/79/49/30/22 个）。
阶段 B 规则（`lib/rules-engine.ts` ↔ T2）：13 条 facts 逐条一致、notCovered 0、exit 0、
digest `c6d782b4…` 全同。**规划书 1.3.2 的 DEFERRED 判据由此闭合。**

**4. ⛔ 阻断性结论：本机跑不了 `next build` 也跑不了 `next dev`（不是代码问题）**
根因两层，都已定位到具体代码位置：
- Next 导出流程在 `node_modules/next/dist/export/index.js:207` 先做 `rm(outDir,{recursive,force})`，
  `outDir = path.join(distDir,'export')`（`build/index.js:1761`）。此时 `.next/export` 里有
  **约 50 个文件**（18 条路由 × html+txt + 404 等），触到宿主"单次删除 ≥50 项"的
  `SAFE_DELETE_BULK_CONFIRM_REQUIRED` 闸。
- 即使绕过这一步，宿主的 safe-delete 会用**回收站二进制**执行删除，而它在连续删除下会
  `Some operations are aborted`（`next dev` 启动时 `rmdir .next/types` 即因此失败）。
- 已排除的**非绕过**手段：① 每次构建前把 `.next` 整体**移走**（rename 可用）——能避开"陈旧文件 unlink"，
  但避不开导出目录那次删除；② 把 `distDir` 指到别处——删除失败是本机整体行为，与卷无关（已实测：
  单文件/空目录删除**能成功**，失败只出现在"≥50 项的批量删除"与"连续删除"两种情形）。
- **未采用的做法（明确记下，避免以后重犯）**：用 `CODEBUDDY_SAFE_DELETE_BULK_THRESHOLD` 调高阈值、
  或清空 `NODE_OPTIONS` 让 shim 不注入 —— 两者都是**绕过宿主安全护栏**，已明确不做。
- **不影响线上**：该闸是**本机宿主**的，GitHub Actions 上不存在 → 判据 2.8 的部署路径完好。
- 已取得的等价证据：`next build` 的**编译 ✓、类型检查 ✓、静态页生成 21/21 ✓** 全部通过，
  失败只发生在生成之后的"导出目录清理"一步。

**5. 进程自查（用户规定）**
`Get-CimInstance` 列出 7 个 `node.exe`，**全部是宿主/平台自身进程**
（`信息收集系统` 的 `npx @deepseek-ai/dsh web`、`chrome-devtools-mcp` ×3；`weixinpay` 插件 ×2）——
**无一处是我启动的**；3000/3100/3200 端口无监听。我拉起的 dev/build 进程均已退出。

**6. 仍未清掉的一处残留**
`web/public/fonts.staging`（227 个空壳文件，是 `sync_fonts.py` 早期版本失败留下的）：
**删不掉也改不了名**（`WinError 5` 句柄占用 + 回收站不可用）。已确认 `.gitignore` 含 `*.staging/`，
故**不会进库**；唯一影响是 `next build` 会把它拷进 `out/`（约 +5.5 MB）。
用户一条命令即可清：`Remove-Item -LiteralPath "web\public\fonts.staging" -Recurse -Force`
（或 `python -c "import shutil;shutil.rmtree('web/public/fonts.staging')"`，视钩子状态而定）。

### 八、11:07 起：用户指示「受阻时提权」→ 提权执行 + 根因收敛 + 拿到可看的预览

**1. 用户裁决（新增协作口径，重要）**：超过 50 项的批量删除，**向用户提交申请、由用户审批**；
遇到阻碍时**向用户提权**，不要卡住不动。执行方式：Bash 调用带 `dangerouslyDisableSandbox: true`
→ 宿主弹出授权 → 用户批准。

**2. 提权的实际效果（已实测）**
- 提权后**批量闸确实放行**（不再报 `BULK_CONFIRM_REQUIRED`），三道门禁 + 编译 + 19 页预渲染全部成功。
- 但暴露出更深一层：删除动作最终落到**
  `trashViaBinary` → `H:\LearnBuddy\resources\vendor\genie-trash\win32-x64.exe`**，
  它在本机报 `Some operations are aborted`（`trashItem` → `tryTrash`）。
- **因此这不是权限问题**：授权能过闸，过不了"回收站二进制本身失败"。
  实测边界：**单个文件的 unlink、空目录的 rmdir 都能成功**；失败只出现在
  「一个目录里几十个文件」这一量级 —— 与 shim 的 `execFileSync(..., {timeout: 5000})`
  **5 秒超时**吻合（E: 盘较慢 + 逐项 trash）。
- ⚠️ **一次失败会污染 `.next`**：构建在预渲染**之后**才失败，但回收站已把 `.next` 里的东西搬走一部分。
  实测第 5 次尝试把 `.next/server/app` 从 19 个 html 打成 4 个文件 → **需要重跑一次构建才能恢复**。
  结论：**每次尝试构建前都必须先把 `.next` 整体移走**，且失败后要重跑一次才能拿到完整预渲染产物。

**3. 已确认可用的两条正经出路**
- **① 用户在自己的终端里跑**：拦截器是通过
  `NODE_OPTIONS=--require=.../genie-safe-delete.cjs` **注入给 agent 拉起的进程**的
  （已实测读出该变量）。用户自己开的终端不带它 → `cd web && npm run build && npm run preview` 必然能过。
  **这是最稳的一条，也是判据 2.8「本地零配置可跑」的最终验证方式。**
- **② 修复宿主的 safe-delete 设置**（关闭该保护 / 修好回收站 / 放宽超时）。

**4. 本轮实际拿到的交付级证据**
`next build`：`✓ Compiled successfully` → `Checking validity of types` → **`✓ Generating static pages (21/21)`**；
`.next/server/app` 下 **19 个 HTML**、`.next/static` **26 个文件**。失败仅发生在之后的"导出目录清理"。

**5. 为让用户能看到界面，我另起了一个**只看不改**的预览服务**
`.work/preview-server.mjs`（**过程目录，不进仓库、不参与交付链路**）：直接服务
`.next/server/app/*.html`（`/grade/` → `grade.html`；`/report/x/` → `report/x.html`）、
`.next/static/*`、`public/*`，并把 `<route>/index.txt` 映射到 `<route>.rsc` 以支持客户端导航。
**实测 16 个端点全部 200**（7 个页面路由 + 5 个数据资产 + 字体 css + woff2），页面标题正常。
⚠️ **它不是 `out/` 的替代品**，交付与判据仍以 `npm run build` 的 `out/` 为准。

### 九、11:18 起：用户四条指令执行 —— 按计划审查产物 + 同步总纲

**1. 用户新增的协作口径（已写入用户级 `~/.learnbuddy/MEMORY.md` 第 10 条）**
**事不过三，受阻即转交**：同一问题最多自行尝试三次；三次不成立刻停下，把「试过什么 / 每次失败证据 /
卡在哪一层 / 需要你做什么」摊清交给用户，**不再自行换路径试探**；**禁止绕过宿主/系统的安全机制**
（调高删除阈值、清空 `NODE_OPTIONS` 让拦截器不注入等）——那是绕护栏不是解决问题。
权限类阻塞**主动提权审批**；审批通过后仍失败 → 说明不是权限问题，如实说明并转交。

**2. 按 2026-09-26 批准的计划（`~/.learnbuddy/plans/stellar-thunder-turing.md`）逐条审查产物**

| 段 | 计划项 | 实际 |
|---|---|---|
| ① | 骨架 / basePath 默认空 / 契约 1.2.0 补齐 / contract-check / sync-assets / data.ts 构建期校验 / 换肤+字体 / build 串联 | **✅ 全部落地** |
| ② | 2.3 五件套页 | **✅ 落地**（数字机械抽取） |
| ② | 2.4 两次点击内可见凭据 | **🟡 部分**：`blockRef` 已在 `score-item-card` 渲染（4 处）；但 **`ProvenanceBlock` 未传 `fingerprintCheck`**（指纹现场复算未接入）、**结果页未展示 `total`**（`weightIncluded`/`upperBound`/`isPartial` 各 0 处） |
| ② | 2.5 上传核查（docx/规则引擎/语料） | **✅ 落地**（跨端对拍 12/12 为证） |
| ③ | 2.6 深链三级降级 | **⬜ 未做**（全仓 `learnbuddy://` 仅出现在 `schema.ts` 注释里，无深链按钮组件） |
| ③ | 2.7 `/eval` 浏览器端现算 + 档位一致率 + ECE + SVG | **🟡 部分**：`use client` = 0（仍是构建期算）；只有 MAE 与逐项档位命中率；**无档位一致率、无 ECE**；无 SVG 自绘；`lib/eval.ts` 未建（用移植的 `analysis.ts`） |
| ③ | 2.8 workflow / .gitignore / README / 本地零配置 | **🟡 文件全备**；🟡 本地因宿主拦截产不出 `out/`；⬜ 线上链接待建仓；`LICENSE` 用户自加 |

**与计划骨架的 5 处落地差异**（均为等价替换或新增，已写进总纲 §六）：`contract-invariants.ts` 未独立成文件
（写在 `superRefine`）；`lib/eval.ts` 未建；新增 `corpus.generated.ts` 与 `base-path.ts`；
`upload-dropzone.tsx` → `upload-inspector.tsx`，`evidence-drawer.tsx` 与 `charts/` 未建；
`scripts/` 新增 `check-fonts.mjs` / `audit-cross-end.mjs` / `sync_fonts.py`。

**2.2 判据自查（源级证据，因无 `out/` 只能查源码）**：`fetch(`/`XMLHttpRequest` **0 处**；
`openai|anthropic|langchain` 全场仅 **1 处**且是 `schema.ts` 的**禁止性 docstring**；
运行时依赖 = `class-variance-authority / clsx / lucide-react / next / react / react-dom / tailwind-merge / zod`，
**无图表库、无 AI SDK**。`/upload` 的语料走 `lib/corpus.generated.ts` 内联（83 KB，不点上传不下载）。

**3. 总纲同步（§五 / §六 / §八 / §九，均属授权可自行编辑的非冻结区）**
- **§五 路径二**：由「⬜ 未开始」改为「🟡 第①段 + 2.3/2.5 已落地；2.4 部分；2.6/2.7 未做」，
  含已落地 / 已实测 / 未闭合三段与「规划书 1.3.2 DEFERRED → PASS」。
- **§六**：`web/` 实际结构并入「✅ 已落地」树；从「⬜ 规划中」移除 `web/`，
  并**顺带修正一处原有事实错误**：`docs/` 早已落地却仍列在「规划中」→ 一并移除；
  记录 5 处落地差异；更新 git 说明（`.gitignore`/`deploy.yml` 已补，`LICENSE` 用户自加）。
- **§八**：新增**宿主的 safe-delete 拦截器**整条取证（注入方式 / 两类失败 / 提权效果 / 对 `next build`
  的直接影响 / 失败会污染 `.next` / 已取得的等价证据 / 两条正经出路 / 明确不做绕过）。
- **§九**：新增 **C-034** 行（sessionId `faf17789-2885-49e6-ac87-2b3b8d4f03bc`），
  并更新「对话 ID 说明」注明本会话**因连接质量差多次中断**。

**4. 进程释放**：`.work/preview-server.mjs` 预览服务已停止（实测 3100 无响应、无监听）。
如需再看：`node .work/preview-server.mjs --port 3100`（约 2 秒起）。`fonts.staging` 已由用户手动删除，确认无误。

### 十、11:27 起：按点名顺序补 2.6 → 2.4 → 2.7 + 修 `.bin`

**1. `web/node_modules/.bin` 为空 —— 已修（根因 + 修法，可复用）**
根因：此前 `npm install` 被宿主 safe-delete 打断，`.bin` 里只剩 **75 个 npm 原子写临时文件**
（`.next.cmd-CJHbRNaw` / `.next.ps1-3Dbfd2sp` 这种 `.<名>.<ext>-<8位随机>` 形态），**真 shim 一个都没生成**。
修法：**从 v0.1 的 `frontend/node_modules/.bin` 整目录复制过来** —— 前提是
① 两侧依赖版本逐包一致（此前已核对）；② **npm 的 shim 全是相对路径** `%~dp0\..\<pkg>\dist\bin\<entry>`，
无绝对路径（已 grep 验证）。实测 `next --version` = 15.1.12、`tsc --version` = 5.7.3、
`npm run typecheck` 正常。3 个残留临时文件已清（钩子报「失败」但实际删除成功 —— 老规律：**报错不代表没做成**）。
⚠️ 判据：**`.bin` 里出现 `<名>-<8位随机后缀>` 就是 npm 被中断**，不要改用 `npm rebuild`（同样会删）。

**2. 2.6 深链入口（新组件 `components/deep-link-button.tsx`）**
三级降级全部实装：① 顶层真实 `<a href="learnbuddy://task?action=start&prompt=…">`；
② 点击后 1200ms 内探 `visibilitychange`/`blur`，仍可见即判未装客户端 → 剪贴板 + 「再试深链」
+ `<details>` 手动复制全文；③ 兜底 `Link` 到 `/report/[id]`。**任何分支都有可见结果。**
**关键设计判断**：`SYSTEM_PROMPT.md` 1.6 万字节、深链 prompt 上限 8000 字符 → **全文内联不可行**，
prompt 只带「目标 + 任务 + 提示词 raw 地址」。`REPO_RAW_BASE` **留空**（仓库没建，**不编造仓库名**），
运行时按 GitHub Pages 站点推导 `raw.githubusercontent.com/<owner>/<repo>/HEAD`，推不出就降级为
「向用户索取提示词」。实测深链 prompt 解码 **162 字符**。
⚠️ HTML 细节：React 会把属性里的 `&` 序列化成 `&amp;`，**用正则从 HTML 里抠深链 href 时不能写裸 `&`**。

**3. 2.4 补接入**：结果页接 `getFingerprintCheck(report.id)`（页面本就是 async）→ `ProvenanceBlock`
显示「复算一致」徽标 + 复算值；新增 `total` 口径面板（已计入/未计入权重、上界、是否部分分、未计入项）。

**4. 2.7 补两指标（口径必须与工具链同源，不能另起一套）**
`lib/analysis.ts` 新增 `CONF_BUCKETS` / `buildLevelPairs` / `computeLevelAgreement` / `computeCalibration`，
**逐条照抄** `evaluate.py` 的 `comparable_pairs` / `compute_item_hits` / `compute_calibration`；三条易错点：
① **可比对** = 该份 status ok 且该项两侧都有档位；命中 = 档位相等；
② **档位一致率是汇总值**（Σhits/Σcomparable），不是逐项命中率的平均；
③ **ECE 用「已展示的 2 位小数」的 accuracy 与 meanConfidence 之差再加权**（先 round2 后加权，顺序不能反）。
`/eval` 新增「置信度校准（ECE）」整区（分桶表 + 可手工复算的加权展开式）。
**顺手纠一处标签错**：原 StatCard 名为「逐项档位命中率」，其值其实是汇总值 → 正名 **档位一致率**，
逐项值仍在「逐项命中率」页签内。另加**两条构造路径交叉校验**（`itemHits` 与 `levelPairs` 的 Σ 不等即 `throw`）。

**5. 验收实测**
`tsc --noEmit` **0 错**；四道门禁全绿；**2.7 两指标与工具链基线逐值相等** —— 一致率 **0.74**（106/144）、
ECE **0.12**、三个桶（low 2 / medium 26 / high 116）的 n/平均置信度/命中率/校准差**逐值全同**
（`.work/verify_2_7.mjs`；该脚本**刻意不经 `data.ts`** —— 那里用了 `@/lib/...` 别名，原生 ESM 解析不了）；
**渲染核验 11/11**（`.work/check_render.mjs` 对预渲染 HTML 断言深链 href / 按钮文案 / total 面板 /
指纹结论 / ECE 数值与展开式）。
**顺带学到**：**只引 `import type` 的模块可被 Node 原生 ESM 直接加载**（类型被剥离），
而引了 `@/lib/...` 值导入的不行 —— 写校验脚本时优先挑前者，或干脆直接读资产文件。

**6. 我又认领一处回归（重要，别再犯）**
10:46 那次 `cp` 把 v0.1 的 5 个页面覆盖回来，**把此前轮次的换肤结果冲掉了**：
全仓残留 **54 处硬编码调色板**（`text/bg/border-{amber,emerald,rose,sky,slate}-NNN`），
分布在 `app/{page,grade,eval,trace,report/[id]}`。上一轮我只修了它引发的 5 处**类型**错误，
**视觉层损失直到本轮审查才检出**。按「改动必须点名」纪律**未擅自修**，已列为最高优先级待确认，
并把总纲 §五 原先那句「94 处已全部改写」**更正为不实描述**。
⚠️ 教训升级：**`cp` 覆盖式移植之后，必须做一次"非类型层面"的审查**（视觉 token、文案口径、设计裁决），
`tsc` 绿不代表交付物没退化。

**7. 总纲同步**：§五 路径二整行重写（2.1–2.7 落地 / 已实测 / 未闭合五项，含 2.7「构建期计算而非
浏览器端现算」的口径偏差与调色板残留）；§六 两处（组件新增项、落地差异第④条）；
§九 新增 **C-035**（并回退一处我对 C-034 历史行的误改：那一行的组件数应保持当时的 4 个）。

### 十一、11:47 起：调色板清零 + `/eval` 改浏览器端现算 + 零依赖 SVG 图表

**1. 调色板清零：63 处 → 0 处（口径要按宽的正则数，别用窄的）**
⚠️ **计数口径**：我上轮报的「54 处」用了窄正则，**漏掉了 `-50/-700/-800/-900` 这些深浅阶**；
按完整 `(text|bg|border|…)-(amber|emerald|rose|sky|slate|…)-\d{2,3}` 数**实为 63 处**。
修法 —— **不逐处替换，而是先立一个唯一来源**：`lib/constants.ts` 新增

```ts
export const TONE = {
  ok:     { text:'text-foreground', icon:'text-primary',        card:'border-border bg-secondary/30',      bar:'bg-foreground/70', badge:'…' },
  notice: { text:'text-muted-foreground', icon:'…',             card:'border-rule bg-secondary/60',        bar:'bg-rule',          badge:'…' },
  bad:    { text:'text-destructive', icon:'text-destructive',   card:'border-destructive/40 bg-destructive/5', bar:'bg-destructive/60', badge:'…' },
} as const;
export function deltaTone(abs, okMax, noticeMax): ToneKey
export function ratioTone(ratio, okMin, noticeMin): ToneKey
```
三档与 `components/ui/badge.tsx` 的 `accent / warning / danger` **逐字一致**（`accent` = 暖棕轻度着色 /
`warning` = 虚线边 + 弱字色 / `danger` = 赭石）；阈值留在调用处（不同指量纲不同）。
另扫全仓**其它** Tailwind 颜色工具类（gray/zinc/blue/violet…）→ **0 处**。
`trace/page.tsx` 里本地定义的 `GRADE_CHIP`/`GRADE_BAR`（emerald/amber/rose 六条）**删掉**，
改走 `CONFIDENCE_GRADE_META`（`●●● / ●●○ / ●○○` 三格刻度 + token chip/bar），把符号也一并显示出来。

**2. `/eval` 改为浏览器端现算（2.7 的口径偏差已闭合）**
切法：`app/eval/page.tsx` 变成**薄服务端页** —— 只读原始数据（每份的金标准逐项档位、AI 逐项档位与置信度、
rubric 权重），交给 `components/eval-metrics.tsx`（`'use client'`）在**运行时**现算
MAE / 命中率 / 一致率 / ECE。于是页面每个数字都能追到一行可查看的 JS，而不是字面量。
- 输入契约 `EvalMetricsInput`（可 JSON 序列化）；`resultStatus` 用 `'ok'|'missing'|'invalid'` 联合类型
  （与 `lib/data.ts` 的 `ResultState` 判别值对齐，否则 `EvalRow.status` 类型对不上）。
- 计算入口 `useEvalMetrics(input)`（`useMemo`），四个面板共用；**两条构造路径的交叉校验仍然保留**
  （`itemHits` 与 `levelPairs` 的 Σ 不等即 throw）。

**3. 零依赖 SVG 图表：4 张（`components/charts/`）**
| 图 | 回答的问题 | 要点 |
|---|---|---|
| `deviation-chart.tsx` | AI 系统性偏松还是偏严 | 从 0 轴左右伸出；**±2 容差带**；带内浅/带外深（深浅阶即程度） |
| `score-comparison-chart.tsx` | 每份差多大 | **哑铃图**：空心=金标准、实心=AI，**两点之间的线段就是差距本身**（不需要读者做减法） |
| `calibration-chart.tsx` | "说有 90% 把握"时真有 90% 对吗 | 可靠性图：对角线=说到做到；**实线=偏自信 / 虚线=偏保守（形而非色）**；点大小 ∝ 样本数 |
| `hit-rate-chart.tsx` | 哪些评分点最易分歧 | 横条 + **70%/90% 两条阈值参考线**（用参考线替代染色） |
全部纯函数组件（无 hook），Server/Client 两侧可渲染；SVG 用 `fill/stroke="currentColor"` + 父级 `text-*` 类，
**不写死颜色**，天然跟随 token。

**4. ⚠️ 一处必须说明的结构改动：`/eval` 从「页签」改为「分区堆叠 + 锚点导航」**
**原因（实测）**：本项目是 `output: 'export'` 的纯静态站，而**页签只渲染当前那个** ——
非活动页签的内容**根本不进 HTML**（实测 `/eval` 里 12 个 `<svg>` 全是图标、`role="img"` 计 0）。
也就是说图会被藏在客户端状态后面：**看不到、也核验不了**（无 JS 时整块缺失）。
改为五个分区（`#sec-section-compare` / `#sec-deviation` / `#sec-calibration` / `#sec-item-hit` /
`#sec-gold-distribution`）+ 页内锚点导航，**所有面板始终在 HTML 里**（实测 5/5 命中，`role="img"` 计 4）。
这是**超出点名范围的结构改动**，已在汇报里显式提出。

**5. 验收（全实测）**
`tsc --noEmit` **0 错**；四道门禁全绿（contract-check / check-fonts 含 **C1 源码文案 926 个非 ASCII 码位
全部覆盖**——新增中文文案都在子集内 / 跨端对拍 12/12）；`next build` **编译 ✓ / 类型检查 ✓ / 21/21 预渲染 ✓**；
**`.work/verify_2_7.mjs` 通过**（一致率 0.74 = 106/144、ECE 0.12、三桶逐值相等，重构后未漂移）；
**`.work/check_render.mjs` 通过**（17 项断言 + 调色板清零 9 路由 0 处 + 9 路由 200 + 深链 prompt 162 字符
+ 4 张 SVG 图）。





